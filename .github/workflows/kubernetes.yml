---
# DISABLED — CI lives in .gitlab-ci.yml; on: {} means this never triggers (inert placeholder).
name: Kubernetes (DISABLED - See GitLab CI)

on: {}

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI runs on GitLab — see .gitlab-ci.yml"
