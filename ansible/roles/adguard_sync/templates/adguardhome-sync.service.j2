# {{ ansible_managed }}
[Unit]
Description=Sync AdGuard Home (dns-01 -> dns-02)
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
Environment=HOME=/var/lib/adguardhome-sync
ExecStart=/usr/local/bin/adguardhome-sync run --config /etc/adguardhome-sync.yaml
