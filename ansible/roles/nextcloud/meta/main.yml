---
galaxy_info:
  role_name: nextcloud
  namespace: weisssrv
  author: Eric Weiss
  description: Nextcloud on a NAS-pinned Debian VM via Docker Compose
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie

# Dependencies are handled in the playbook, not in meta, to allow isolated
# unit testing of this role. The nextcloud playbook applies base,
# postfix_null_client, node_exporter_host, and alloy_host as needed.
dependencies: []
