---
# Role metadata

galaxy_info:
  role_name: plex
  namespace: weisssrv
  author: Eric Weiss
  description: Plex Media Server in LXC with GPU transcoding and Traefik ingress
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie

# Dependencies are handled in the playbook, not in meta, to allow isolated
# unit testing of this role. The plex playbook applies the base role first.
dependencies: []
