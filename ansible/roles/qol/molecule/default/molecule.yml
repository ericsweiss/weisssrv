---
# Molecule configuration for qol (quality of life) role testing
# Tests: package installation, Oh My Zsh setup, neovim config, dotfiles
dependency:
  name: galaxy
  options:
    requirements-file: requirements.yml
driver:
  name: docker
platforms:
  - name: qol-test
    image: registry.git.ericsweiss.com/eric/weisssrv/molecule-test:latest
    pre_build_image: true
    privileged: true
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    command: /lib/systemd/systemd
    stop_signal: SIGRTMIN+3
    tty: true
    tmpfs:
      - /run
      - /tmp
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: ../../..
    # Required for molecule_docker plugin (upstream broken conditional in destroy.yml)
    ANSIBLE_ALLOW_BROKEN_CONDITIONALS: "1"
  config_options:
    defaults:
      callbacks_enabled: profile_tasks, timer
      stdout_callback: ansible.builtin.default
      result_format: yaml
  playbooks:
    prepare: prepare.yml
    converge: converge.yml
    verify: verify.yml
  inventory:
    group_vars:
      all:
        admin_user: testuser
        # Skip base role configs that don't work in containers
        skip_ssh_config: true
        skip_dns_config: true
        skip_timezone_config: true
        ssh_authorized_keys: []
        qol_packages:
          - zsh
          - neovim
          - fzf
          - ripgrep
          - htop
          - tmux
        omz_plugins:
          - git
          - sudo
          - history
          - zsh-autosuggestions
          - zsh-syntax-highlighting
          - fzf
          - docker
verifier:
  name: ansible
scenario:
  test_sequence:
    - dependency
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - cleanup
    - destroy
