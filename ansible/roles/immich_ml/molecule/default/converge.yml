---
# Converge: render the Immich ML stack in skip-deploy mode (no GPU, Docker, or
# service management in the CI container).
- name: Converge
  hosts: all
  become: true
  roles:
    - role: immich_ml
      vars:
        skip_immich_ml_deploy: true
