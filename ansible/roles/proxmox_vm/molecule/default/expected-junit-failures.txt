# Negative-path guard tasks this scenario DELIBERATELY drives to failure
# (block/rescue exercises). The junit callback records the raw failure even
# though the rescue + assertions pass; CI's sanitize-junit step downgrades
# EXACTLY these names in the report. Job status is the arbiter.
ansible/roles/proxmox_vm/molecule/default/expected-junit-failures.txt
Validate resource pool is in standard list
Fail if required storage pool does not exist
Attach additional disks to VM
Assert every additional disk pins a unique, explicit scsi_slot
