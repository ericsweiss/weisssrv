---
# K3s installation

- name: Install k3s prerequisites
  ansible.builtin.apt:
    name:
      - curl
      - open-iscsi
      - nfs-common
    state: present
    update_cache: true
  when: not (skip_k3s_install | default(false))

- name: Install k3s prerequisites (minimal for config testing)
  ansible.builtin.apt:
    name:
      - curl
    state: present
    update_cache: true
  when: skip_k3s_install | default(false)

- name: Enable iscsid service
  ansible.builtin.systemd:
    name: iscsid
    enabled: true
    state: started
  when: not (skip_k3s_install | default(false))

- name: Create k3s config directory
  ansible.builtin.file:
    path: /etc/rancher/k3s
    state: directory
    mode: "0755"

# Pin the GitLab registry hostname to the internal Traefik VIP so containerd
# resolves it locally (no upstream DNS) and pulls CI images via the direct
# internal path. See k3s_registry_host_pins in defaults/main.yml for the why +
# the post-deploy verification. Idempotent; empty list is a no-op.
- name: Pin container-registry hostnames to the internal Traefik VIP in /etc/hosts
  ansible.builtin.blockinfile:
    path: /etc/hosts
    marker: "# {mark} ANSIBLE MANAGED: k3s registry host pins"
    block: |
      {% for pin in k3s_registry_host_pins %}
      {{ pin.ip }} {{ pin.name }}
      {% endfor %}
  when:
    - k3s_registry_host_pins | default([]) | length > 0
    - not (skip_k3s_install | default(false))

# Mount additional persistent disks (ZFS zvols attached as SCSI devices by proxmox_vm role)
- name: Mount additional persistent disks
  ansible.builtin.include_role:
    name: zvol_mount
  vars:
    zvol_mount_disks: "{{ vm_additional_disks | default([]) }}"
  when: vm_additional_disks is defined and vm_additional_disks | length > 0

- name: Include server tasks
  ansible.builtin.include_tasks: server.yml
  when: k3s_role == 'server'

- name: Include agent tasks
  ansible.builtin.include_tasks: agent.yml
  when: k3s_role == 'agent'
