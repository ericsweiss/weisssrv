---
galaxy_info:
  role_name: gitlab
  namespace: weisssrv
  author: Eric Weiss
  description: GitLab EE (CE features) installation and configuration
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie

# Dependencies are handled in the playbook, not in meta, to allow
# isolated unit testing of this role. The gitlab playbook applies
# base and postfix_null_client roles as needed.
dependencies: []
