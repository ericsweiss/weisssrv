---
# Base configuration playbook

- name: Deploy base configuration
  hosts: base_managed
  become: true
  roles:
    - role: base
      tags: [base]
    - role: nic_tuning
      tags: [nic_tuning, network]
