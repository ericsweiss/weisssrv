---
# DEPRECATED — DO NOT RUN. The old helm-upgrade flow lives in git history.
# Platform Helm charts are now Flux-managed. Update workflow:
#   1. Edit versions in ansible/inventories/prod/group_vars/all.yml
#   2. Run: task flux:sync-versions
#   3. Commit and push — Flux reconciles the new chart versions.
# See docs/29-flux-operations.md § "Updating an App Version".

- name: Update k3s Helm charts
  hosts: localhost
  gather_facts: false
  connection: local
  vars:
    ansible_become: false
  tasks:
    - name: DEPRECATED — use Flux
      ansible.builtin.fail:
        msg: "This playbook is deprecated. Edit versions in all.yml, run 'task flux:sync-versions', commit and push."
