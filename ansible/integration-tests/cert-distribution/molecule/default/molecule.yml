---
# Certificate Distribution Integration Test
# Tests: acme_certs with SSH-based distribution to multiple hosts

driver:
  name: docker

platforms:
  - name: dns-01
    image: registry.git.ericsweiss.com/eric/weisssrv/molecule-test:latest
    pre_build_image: true
    privileged: true
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    command: /lib/systemd/systemd
    stop_signal: SIGRTMIN+3
    tty: true
    tmpfs:
      - /run
      - /tmp
    networks:
      - name: cert-stack-net
    groups:
      - cert_stack
      - cert_servers

  - name: cert-client-01
    image: registry.git.ericsweiss.com/eric/weisssrv/molecule-test:latest
    pre_build_image: true
    privileged: true
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    command: /lib/systemd/systemd
    stop_signal: SIGRTMIN+3
    tty: true
    tmpfs:
      - /run
      - /tmp
    networks:
      - name: cert-stack-net
    groups:
      - cert_stack
      - cert_clients

  - name: cert-client-02
    image: registry.git.ericsweiss.com/eric/weisssrv/molecule-test:latest
    pre_build_image: true
    privileged: true
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    command: /lib/systemd/systemd
    stop_signal: SIGRTMIN+3
    tty: true
    tmpfs:
      - /run
      - /tmp
    networks:
      - name: cert-stack-net
    groups:
      - cert_stack
      - cert_clients

provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: ../../../../roles
    # Required for molecule_docker plugin (upstream broken conditional in destroy.yml)
    ANSIBLE_ALLOW_BROKEN_CONDITIONALS: "1"
    # Provide dummy SSH key for testing (base role expects this via all.yml lookup)
    # yamllint disable-line rule:line-length
    SSH_PUBLIC_KEY: "ssh-rsa  test@molecule"
  playbooks:
    prepare: ../../../_shared/prepare.yml
  inventory:
    group_vars:
      cert_stack:
        # Common SSH configuration
        admin_username: eric

      cert_servers:
        # acme.sh configuration
        acme_email: test@example.com
        acme_cloudflare_token: test_cloudflare_token_not_real
        # Test-local list driving the mock cert fixtures in converge/verify —
        # NOT a role knob (acme_certs issues internal_domain + wildcard
        # directly and reads no domain-list variable).
        mock_cert_domains:
          - domain: "*.esweiss.com"
          - domain: esweiss.com

        # SSH keys (dns01_ssh_private_key, dns01_ssh_public_key) are generated
        # at runtime in converge.yml and injected onto dns-01's hostvars via
        # add_host (host facts override inventory group_vars). They're
        # intentionally not defined here — the role's assert surfaces a clear
        # error if the runtime injection ever fails.

        # Certificate distribution targets — host_key is added at runtime in
        # converge.yml (slurped from each cert_clients container) and the
        # whole list is re-injected onto dns-01 via add_host so the role
        # gets the full structure. We keep a copy of the static fields here
        # so verify.yml (which runs as a separate ansible-playbook invocation,
        # without converge's add_host facts) can still read the list shape.
        cert_distribution_targets:
          - host: cert-client-01
            ip: cert-client-01
            cert_dir: /etc/ssl/certs/wildcard
            owner: root
            group: root
            cert_mode: "0644"
            key_mode: "0600"
            restart_service: ssh
          - host: cert-client-02
            ip: cert-client-02
            cert_dir: /etc/ssl/certs/wildcard
            owner: root
            group: root
            cert_mode: "0644"
            key_mode: "0600"
            restart_service: ssh

verifier:
  name: ansible

scenario:
  test_sequence:
    - dependency
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    # Skip idempotence: a fresh throwaway keypair is generated per run, so
    # the deployed key files always change between converges
    # - idempotence
    - verify
    - cleanup
    - destroy
