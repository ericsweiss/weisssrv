# This manifest was generated by flux. DO NOT EDIT.
# POST-MERGE ACTION REQUIRED: after this MR merges to main, change
# `branch: flux/migration` below to `branch: main` (via a follow-up commit
# on main), then delete the flux/migration branch on the remote. Until that
# switch happens, Flux continues reconciling flux/migration even after merge.
# See docs/29-flux-operations.md § "Post-merge branch switch" for the exact
# runbook.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: flux/migration
  secretRef:
    name: flux-system
  url: https://git.ericsweiss.com/eric/weisssrv.git
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/clusters/weisssrv
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
