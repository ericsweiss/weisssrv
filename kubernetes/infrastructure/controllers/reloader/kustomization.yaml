---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Single-namespace dir: set the namespace so the shared default-deny-ingress
# component (added below) lands in reloader.
namespace: reloader
resources:
  - namespace.yaml
  - release.yaml
  - networkpolicy.yaml

components:
  - ../../../components/netpol-baseline
