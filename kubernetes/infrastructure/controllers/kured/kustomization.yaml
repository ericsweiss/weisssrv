---
# No netpol-baseline component: kured lands in kube-system, whose default-deny
# and whose allow for kured's own :8080 metrics port live together in
# ../../configs/kube-system-policies/.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml
