---
# VPA: Initial for the single stateless Deployment — right-size cpu+memory on
# natural restarts, no proactive eviction of the live VPN. Small ceiling; wg-easy
# is a lightweight Node process.
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: wg-easy
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: wg-easy
  updatePolicy:
    updateMode: Initial
  resourcePolicy:
    containerPolicies:
      # 0.8x the container's 256Mi memory limit (deployment.yaml): the policy
      # also controls limits, so a cap at the limit would leave no ceiling that
      # ever binds (docs/33 § Limit oscillation).
      - containerName: wg-easy
        minAllowed: {cpu: 25m, memory: 64Mi}
        maxAllowed: {cpu: 500m, memory: 204Mi}
        controlledResources: [cpu, memory]
      # Leave the one-shot privileged sysctl initContainer alone.
      - containerName: sysctl
        mode: "Off"
