---
# TLS certificates for Recipes namespace (Mealie + Bar Assistant)
# These must be in the same namespace as the IngressRoutes that reference them

# External domains - food.ericsweiss.com, bar.ericsweiss.com
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: recipes-tls
  namespace: recipes
spec:
  secretName: recipes-ericsweiss-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "food.${cluster_external_domain}"
  dnsNames:
    - "food.${cluster_external_domain}"
    - "bar.${cluster_external_domain}"
  renewBefore: 720h

---
# Internal domains - food.esweiss.com, bar.esweiss.com
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: recipes-tls-internal
  namespace: recipes
spec:
  secretName: recipes-esweiss-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "food.${cluster_internal_domain}"
  dnsNames:
    - "food.${cluster_internal_domain}"
    - "bar.${cluster_internal_domain}"
  renewBefore: 720h
