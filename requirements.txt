# Python dependencies for Ansible and Molecule testing.
#
# Exact pins, not lower bounds: a local checkout and CI must resolve the same
# toolchain, and a lower bound would silently pull a new major. The molecule
# test/CI images are built in weisssrv-lib (docker/molecule-ci/) from their own
# pins — this file is the LOCAL toolchain plus the source of `ansible`, which
# `.gitlab-ci.yml`'s `variables.ANSIBLE_VERSION` repeats for the jobs that
# pip-install it directly. scripts/test_site_configs.py asserts those two agree.
ansible==11.6.0
ansible-lint==25.12.2
# black is a transitive dep (ansible-lint's jinja rule imports it); the black
# 26.5.x wheels ship a broken mypyc extension that makes ansible-lint fail to
# load. Lift once 26.5.x is fixed.
black<26.5.0
molecule==26.4.0
molecule-plugins[docker]==25.8.12
yamllint==1.38.0
