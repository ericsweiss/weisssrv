# GitLab CI/CD Pipeline for weisssrv Homelab Infrastructure
#
# Canonical source: https://git.ericsweiss.com/eric/weisssrv
# GitHub mirror: https://github.com/ericsweiss/weisssrv (read-only)
#
# Restart/reboot policy:
# - Service restarts: automatic (during deploys)
# - Machine reboots: manual approval required (maintenance jobs auto-reboot when triggered)
#
# Required CI/CD Variables (Settings > CI/CD > Variables):
# - OP_SERVICE_ACCOUNT_TOKEN (masked, protected) - 1Password service account
#
# Optional CI/CD Variables:
# - GH_API_TOKEN (masked) - For version checker API rate limits
#
# Pipeline triggers:
# - Merge requests: Validation only (no deploy)
# - Push to main: Validation + auto-deploy
# - Scheduled: Version checking and secret detection only (all other jobs excluded
#   via when: never), EXCEPT a schedule with SCHEDULE_TYPE=full-test, which also runs
#   build-molecule-images + molecule/integration tests as an external-dependency canary
# - Manual (web): Lint, validate, test, security stages only (deploy/gate/maintenance excluded)

stages:
  - build
  - lint
  - validate
  - test
  - security
  - ai-review
  - gate
  - deploy
  # All maintenance jobs (6 ops, maintenance-run-all wrapper, and the
  # ad-hoc maintenance-verify cluster-health button) are in this single
  # stage. Each maintenance op embeds the verify script as its final
  # step (see scripts/post-maintenance-verify.sh) so verify always runs
  # right after the op in the same CI job — no cross-job needs/stage
  # gymnastics, and no GitLab manual-job semantic quirks to navigate.
  - maintenance

# ============================================================================
# Default settings
# ============================================================================
default:
  image: python:3.11-slim
  # All weisssrv jobs run on the privileged infrastructure runner.
  # The shared non-privileged runner (k8s-deploy) is for other GitLab projects.
  tags: [infrastructure]
  # Cancel jobs when the same branch gets a newer pipeline. Deploy/maintenance
  # jobs override this to false to ensure they always complete.
  interruptible: true

# ============================================================================
# Global variables
# ============================================================================
variables:
  # Single source for the Ansible pin used by the lint (.ansible-base) and
  # deploy (.deploy-base) pip installs. Keep in step with requirements.txt's
  # ansible pin (which the molecule-ci image builds from).
  ANSIBLE_VERSION: "11.6.0"
  # Single source for the kustomize pin used by flux-lint and deploy-verify.
  KUSTOMIZE_VERSION: "5.4.3"
  KUSTOMIZE_SHA256: "3669470b454d865c8184d6bce78df05e977c9aea31c30df3c669317d43bcc7a7"
  # Single source for the pytest/PyYAML pins pip-installed by the lint/test/
  # verify jobs. PYYAML_VERSION also matches the version-check job's pin so
  # every job parses YAML with the same library version.
  PYTEST_VERSION: "9.1.1"
  PYYAML_VERSION: "6.0.2"

# ============================================================================
# Workflow rules - when to run the pipeline
# ============================================================================
workflow:
  # Aggressively cancel superseded MR pipelines. GitLab's default auto-cancel is
  # "conservative": it stops cancelling a pipeline the moment any NON-interruptible
  # job (build-molecule-images, the deploy-* jobs) has started — so on a busy MR,
  # every push left the previous pipeline's heavy but interruptible molecule/
  # integration jobs running, piling up on the shared k3s runners and starving
  # them (the systemd-in-DinD containers then time out — the "Container is not
  # running" flakes get worse the more commits are pushed). on_new_commit:
  # interruptible cancels those interruptible jobs on the next push while leaving
  # the non-interruptible image-build/deploy jobs untouched.
  auto_cancel:
    on_new_commit: interruptible
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
    - if: '$CI_PIPELINE_SOURCE == "web"'

# ============================================================================
# Templates for reuse
# ============================================================================
.ansible-base:
  variables:
    # GitLab checkout is world-writable, causing Ansible to ignore ansible.cfg
    # Use absolute roles path; config is copied to a secure temp file in before_script
    ANSIBLE_ROLES_PATH: ${CI_PROJECT_DIR}/ansible/roles
  before_script:
    - apt-get update -qq
    - apt-get install -y -qq git
    - |
      # Copy ansible.cfg to unpredictable temp path (avoids symlink attacks in /tmp)
      export ANSIBLE_CONFIG="$(mktemp /tmp/ansible-XXXXXXXXXX.cfg)"
      install -m 600 ansible/ansible.cfg "$ANSIBLE_CONFIG"
      trap "rm -f '$ANSIBLE_CONFIG'" EXIT
    # black<26.5.0: ansible-lint's jinja rule imports black, and the black 26.5.x
    # wheels ship a broken mypyc extension (ImportError: undefined symbol
    # CPyMndule_black___resources) that makes ansible-lint fail to load. Pin below
    # it (ansible-lint only needs black>=24.3.0); lift once 26.5.x is fixed.
    - pip install --quiet "ansible==${ANSIBLE_VERSION}" ansible-lint==25.12.2 'black<26.5.0'
    - ansible-galaxy collection install -r ansible/requirements.yml

.install-1password:
  before_script:
    - apt-get update -qq
    - apt-get install -y -qq curl gnupg openssh-client
    - |
      # Install 1Password CLI
      curl -sS https://downloads.1password.com/linux/keys/1password.asc | \
        gpg --dearmor --output /usr/share/keyrings/1password-archive-keyring.gpg
      echo "deb [arch=amd64 signed-by=/usr/share/keyrings/1password-archive-keyring.gpg] https://downloads.1password.com/linux/debian/amd64 stable main" \
        > /etc/apt/sources.list.d/1password.list
      apt-get update -qq && apt-get install -y -qq 1password-cli
    - op --version

.install-1password-alpine:
  before_script:
    - apk add --no-cache curl bash gnupg jq
    - |
      # 1Password's apk repo is signed with their published RSA key; fetch the
      # public key into /etc/apk/keys under its exact filename so apk can verify
      # the index. The older `curl 1password.asc | gpg --dearmor` form broke once
      # 1Password moved to this RSA key — apk reported "UNTRUSTED signature" and
      # 1password-cli became unselectable. See 1password.dev/cli/get-started.
      curl -sSf https://downloads.1password.com/linux/keys/alpinelinux/support@1password.com-61ddfc31.rsa.pub \
        -o /etc/apk/keys/support@1password.com-61ddfc31.rsa.pub
      echo "https://downloads.1password.com/linux/alpinelinux/stable/" >> /etc/apk/repositories
      apk update
      apk add --no-cache 1password-cli
    - op --version

# Shared "never on scheduled or web pipelines" rule preamble. Deploy/
# maintenance/gate jobs start their rules with this so the policy lives in
# one place. Pull in via `- !reference [.skip-schedule-web, rules]`.
.skip-schedule-web:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: never

# Shared rules for every manual maintenance-stage job: skip schedule/web
# pipelines, then a manual button on main (op-token guarded). Pull in via
# `rules: - !reference [.maintenance-manual-rules, rules]` (nested !reference
# to .skip-schedule-web is fine — see deploy-verify's rules for precedent).
.maintenance-manual-rules:
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      when: manual
      # GitLab quirk: rules-based `when: manual` defaults to
      # `allow_failure: false` (top-level `when: manual` defaults to
      # true). Without this explicit `allow_failure: true`, every main
      # pipeline would PAUSE waiting for someone to play these manual jobs
      # — blocking unrelated CI flow. Failures are still surfaced in the
      # GitLab UI when a job is actually played.
      allow_failure: true

# Shared `changes:` path lists for the lint/validate/test jobs. Each job's
# merge_request_event and main rules pull the SAME list via
# `changes: !reference [.paths-<x>, changes]`, so a path edit happens once
# instead of in two hand-synced copies (which silently drift — a job just stops
# running on one trigger). Hidden jobs (dot-prefixed) are template fragments,
# never scheduled. NOT plain YAML anchors: the pipeline stays anchor-free
# (extends/!reference only) so the planned include:-file split remains safe
# (docs/16 ARCH-4 — anchors are file-local and would break across includes).
.paths-build-molecule:
  changes:
    - docker/molecule-test/**/*
    - docker/molecule-ci/**/*
    - requirements.txt
    - ansible/requirements.yml
.paths-shellcheck:
  changes:
    - scripts/**/*
    - ansible/roles/**/*.sh
    - ansible/roles/**/*.sh.j2
.paths-deploy-coverage:
  changes:
    - ansible/roles/**/*
    - ansible/playbooks/**/*
    # Covers group_vars/, host_vars/, hosts.yml, and any other top-level
    # inventory file under ansible/inventories/prod/.
    - ansible/inventories/prod/**/*
.paths-molecule-matrix:
  changes:
    - ansible/roles/**/molecule/**/*
    - ansible/integration-tests/**/*
    - scripts/check-molecule-matrix-coverage.sh
    - .gitlab-ci.yml
.paths-flux-version-pin:
  changes:
    - .gitlab-ci.yml
    - kubernetes/infrastructure/sources/versions-configmap.yaml
    - ansible/inventories/prod/group_vars/all.yml
.paths-yaml-lint:
  changes:
    - ansible/**/*
    - kubernetes/**/*
    - .gitlab-ci.yml
.paths-ansible:
  changes:
    - ansible/**/*
.paths-terraform:
  changes:
    - terraform/**/*
.paths-flux-versions-sync:
  changes:
    - ansible/inventories/prod/group_vars/all.yml
    - kubernetes/infrastructure/sources/versions-configmap.yaml
    - scripts/generate-versions-configmap.py
.paths-hosts-env-sync:
  changes:
    - ansible/inventories/prod/hosts.yml
    - scripts/hosts.env
    - scripts/generate-hosts-env.py
.paths-python-tests:
  changes:
    # The pytest suite validates real tree files beyond scripts/:
    # test_adguard_api_body.py asserts on the adguard_home role, and
    # test_check_molecule_matrix_coverage.py smoke-tests the live matrix.
    - scripts/**/*
    - ansible/inventories/prod/group_vars/all.yml
    - ansible/roles/adguard_home/tasks/api_base_config.yml
    - ansible/roles/**/molecule/**/*
    - ansible/integration-tests/**/*
    - .gitlab-ci.yml
.paths-flux-lint:
  changes:
    - kubernetes/**/*
    - ansible/inventories/prod/group_vars/all.yml
.paths-prometheus-config:
  changes:
    - kubernetes/infrastructure/observability/kube-prometheus-stack/release.yaml
    - kubernetes/infrastructure/observability/kube-prometheus-stack/alertmanager-config.yaml
    - scripts/extract-prometheus-config.py
    - scripts/lint-prometheus-config.sh
.paths-molecule:
  changes:
    - ansible/roles/**/*
    - ansible/molecule/**/*
    - ansible/playbooks/**/*
    - ansible/inventories/prod/group_vars/**/*
    # Test-image/dependency changes must run the suite they feed
    # (build-molecule-images fires on the same paths).
    - docker/molecule-test/**/*
    - docker/molecule-ci/**/*
    - requirements.txt
    - ansible/requirements.yml
.paths-integration:
  changes:
    - ansible/integration-tests/**/*
    - ansible/roles/**/*
    - ansible/inventories/**/*
    - docker/molecule-test/**/*
    - docker/molecule-ci/**/*
    - requirements.txt
    - ansible/requirements.yml

# GitLab HTTP state backend (project-scoped Terraform state + lock) shared by
# terraform-plan and deploy-terraform via `extends`. Edit the path/auth once.
.terraform-http-backend:
  variables:
    TF_HTTP_ADDRESS: "https://git.ericsweiss.com/api/v4/projects/${CI_PROJECT_ID}/terraform/state/cloudflare"
    TF_HTTP_LOCK_ADDRESS: "https://git.ericsweiss.com/api/v4/projects/${CI_PROJECT_ID}/terraform/state/cloudflare/lock"
    TF_HTTP_UNLOCK_ADDRESS: "https://git.ericsweiss.com/api/v4/projects/${CI_PROJECT_ID}/terraform/state/cloudflare/lock"
    TF_HTTP_USERNAME: "gitlab-ci-token"
    TF_HTTP_PASSWORD: "${CI_JOB_TOKEN}"
    TF_HTTP_LOCK_METHOD: "POST"
    TF_HTTP_UNLOCK_METHOD: "DELETE"
    TF_HTTP_RETRY_WAIT_MIN: "5"

.deploy-base:
  extends: .install-1password
  # Deploys touch shared infrastructure; never cancel a running deploy when a new
  # pipeline starts — wait for it to complete instead.
  interruptible: false
  # Runner-pod casualties (scheduling timeout, NoExecute taint eviction
  # when a node degrades mid-job — pipeline 355 lost deploy-ansible-base
  # to DeletionByTaintManager) are environmental, and the playbooks are
  # idempotent: retry instead of failing the whole needs chain.
  retry:
    max: 2
    when:
      - runner_system_failure
  variables:
    ANSIBLE_ROLES_PATH: ${CI_PROJECT_DIR}/ansible/roles
    # alloy_host renders loki.write basic_auth from these and asserts they are
    # non-empty (pipeline 355 silently clobbered the fleet's alloy configs when
    # they were missing). Defined on the shared base so EVERY deploy job that
    # may run alloy_host (e.g. deploy-ansible-mail via site.yml --limit) has
    # them, not just deploy-ansible-base.
    LOKI_PUSH_USER: "op://Homelab/Loki Push Auth/username"
    LOKI_PUSH_PASSWORD: "op://Homelab/Loki Push Auth/password"
  before_script:
    - !reference [.install-1password, before_script]
    - pip install --quiet "ansible==${ANSIBLE_VERSION}"
    - |
      # Copy ansible.cfg to secure temp path (world-writable checkout causes Ansible to ignore it)
      export ANSIBLE_CONFIG="$(mktemp /tmp/ansible-XXXXXXXXXX.cfg)"
      install -m 600 ansible/ansible.cfg "$ANSIBLE_CONFIG"
      trap "rm -f '$ANSIBLE_CONFIG'" EXIT
    - |
      # Setup SSH from 1Password. umask 077 in a subshell so the key file is
      # never created with the default 0644 mode (no chmod race window).
      mkdir -p ~/.ssh
      chmod 700 ~/.ssh
      (umask 077; op read "op://Homelab/SSH Key/private key" > ~/.ssh/id_ed25519)
      # TOFU keyscan: ephemeral runners have no known_hosts to inherit, so we
      # fetch fresh per job rather than bake host keys into the image. Accepted
      # on a single-LAN homelab (MITM would need both network paths in the job
      # window). Strict pinning lives on the operator path: ansible.cfg
      # (StrictHostKeyChecking=accept-new) + host_vars/dns-01.yml host_key for
      # acme_certs cert distribution.
      # Add known hosts for all infrastructure (Proxmox hosts, services, k3s
      # VMs). ALL_SSH_IPS is generated from ansible/inventories/prod/hosts.yml
      # (scripts/hosts.env; the hosts-env-sync job guards it against drift), so
      # a new node is keyscanned automatically without editing this list.
      . scripts/hosts.env
      # shellcheck disable=SC2086  # word-split ALL_SSH_IPS into one keyscan
      ssh-keyscan -H $ALL_SSH_IPS >> ~/.ssh/known_hosts 2>/dev/null || true
    - ansible-galaxy collection install -r ansible/requirements.yml

# Shared kubectl install (pinned checksum) + kubeconfig fetch from 1Password.
# Pulled into .maintenance-base and .k3s-deploy-base via
# `- !reference [.kubectl-setup, before_script]` so the version, checksum, and
# kubeconfig logic live in one place.
.kubectl-setup:
  before_script:
    - |
      # Install kubectl (pinned checksum for supply-chain integrity).
      curl -fsSL "https://dl.k8s.io/release/v1.35.2/bin/linux/amd64/kubectl" -o /tmp/kubectl
      echo "924eb50779153f20cb668117d141440b95df2f325a64452d78dff9469145e277  /tmp/kubectl" | sha256sum -c -
      install -m 0755 /tmp/kubectl /usr/local/bin/kubectl
    - |
      # Kubeconfig from 1Password (base64-encoded to preserve YAML formatting).
      set -eo pipefail
      mkdir -p ~/.kube
      KUBECONFIG_B64=$(op read "op://Homelab/K3s Kubeconfig/kubeconfig")
      if [ -z "$KUBECONFIG_B64" ]; then
        echo "ERROR: op read returned empty kubeconfig. Is OP_SERVICE_ACCOUNT_TOKEN set?"
        exit 1
      fi
      # umask 077 subshell: never create the kubeconfig world-readable.
      (umask 077; echo "$KUBECONFIG_B64" | base64 -d > ~/.kube/config)
      export KUBECONFIG="$HOME/.kube/config"
      echo "Kubeconfig written ($(wc -c < ~/.kube/config) bytes), KUBECONFIG=$KUBECONFIG"

# Maintenance jobs need both ansible (from .deploy-base) and kubectl
# (so each job can call scripts/post-maintenance-verify.sh after its
# playbook). Extends .deploy-base, then layers in the kubectl install
# + kubeconfig setup that .k3s-deploy-base does.
.maintenance-base:
  extends: .deploy-base
  # Maintenance ops are manual, ad-hoc buttons in the last (maintenance) stage.
  # They consume NO artifacts from earlier jobs (all inputs come from 1Password
  # at runtime), so an empty needs lets an operator play them immediately
  # without waiting on the entire build/test/deploy graph to complete first.
  needs: []
  # Cap below the detached self-host reboot's 5400s (90m) fallback timer: GitLab
  # must cancel a runaway job (-> the after_script re-arm fires) BEFORE that timer
  # could go off mid-run and reboot the executor's own host. The longest job
  # (maintenance-run-all) is ~30-45m, so 70m is comfortable headroom.
  timeout: 70m
  before_script:
    - !reference [.deploy-base, before_script]
    - apt-get install -y -qq jq
    - !reference [.kubectl-setup, before_script]
  after_script:
    # If the run armed a DETACHED self-host reboot (the executor's own Proxmox
    # host needed a reboot), re-arm it to fire promptly (+60s) now that the job is
    # ending and disarm the long fallback timer. Runs on success, failure, AND
    # cancel (so a cancelled run can't leave a 90-min timer ticking), and anchors
    # the reboot to the real job-end instead of a fixed-delay guess. No-op if the
    # run armed nothing (marker absent). Best-effort: the long fallback timer
    # armed during the run still covers the reboot if this step fails.
    - bash scripts/maintenance-rearm-self-reboot.sh || true

.k3s-deploy-base:
  extends: .install-1password
  # Use Debian-based image (Alpine musl resolver has issues with k8s ndots:5)
  image: python:3.11-slim
  # k3s deploys/verifies touch the cluster; never interrupt mid-run.
  interruptible: false
  before_script:
    - !reference [.install-1password, before_script]
    # jq is used by deploy-verify to parse kubectl JSON output (Flux CR
    # readiness checks). helm and yq are not installed — Flux manages chart
    # deployments, so no CI job needs them.
    - apt-get install -y -qq jq
    - !reference [.kubectl-setup, before_script]
    # Skipping `kubectl cluster-info` — requires list perms on kube-system
    # services, which the runner ServiceAccount lacks. Per-job kubectl calls
    # target narrower scopes and use the K3s Kubeconfig credentials.
    - kubectl version --output=yaml --request-timeout=5s

# ============================================================================
# BUILD STAGE - Container images
# ============================================================================

build-molecule-images:
  # Pushes images consumed by later jobs in this pipeline; do not interrupt.
  interruptible: false
  # This job pulls a lot of external deps at build time — the base image, apt,
  # pip, ansible-galaxy (galaxy.ansible.com), and the dind daemon — any of which
  # can transiently fail and surface as a script_failure (e.g. a galaxy download
  # 500/timeout, or "Cannot connect to the Docker daemon"). Since the build itself
  # is deterministic (a REAL error still fails after the retries), retry those too
  # so a transient download doesn't block the whole molecule matrix. (Test jobs
  # keep a narrow retry — a failing test must NOT be masked; see .molecule-base.)
  retry:
    max: 2
    when:
      - runner_system_failure
      - scheduler_failure
      - script_failure
  # Builds both CI images:
  # - molecule-test: the container molecule spins up (has all role apt deps)
  # - molecule-ci: the GitLab CI runner image (has docker-cli + molecule + ansible)
  # Only rebuilds when Dockerfiles or requirements change.
  stage: build
  image: python:3.11
  services:
    - docker:24.0-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  before_script:
    # Install the static docker CLI (NOT docker.io: the Trixie docker.io package
    # ships only the daemon, no CLI — see docker/molecule-ci/Dockerfile). Pinned
    # version + sha256 to match the checksum discipline used for every other
    # downloaded binary in this pipeline.
    - |
      ARCH=$(dpkg --print-architecture)
      case "$ARCH" in
        amd64) DOCKER_ARCH=x86_64; DOCKER_SHA256=4f798b3ee1e0140eab5bf30b0edc4e84f4cdb53255a429dc3bbae9524845d640 ;;
        arm64) DOCKER_ARCH=aarch64; DOCKER_SHA256=e6b53725a73763ab3f988c73f8772eaed429754c1a579db5ff11f21990fd1817 ;;
        *) echo "unsupported arch: $ARCH"; exit 1 ;;
      esac
      curl -fsSL "https://download.docker.com/linux/static/stable/${DOCKER_ARCH}/docker-27.5.1.tgz" -o /tmp/docker.tgz
      echo "${DOCKER_SHA256}  /tmp/docker.tgz" | sha256sum -c -
      tar xz --strip-components=1 -C /usr/local/bin -f /tmp/docker.tgz docker/docker
      rm /tmp/docker.tgz
      docker --version
      # Wait for the dind service daemon to accept connections before building;
      # it can lag the job start, otherwise `docker build` fails with
      # "Cannot connect to the Docker daemon at tcp://docker:2375".
      for i in $(seq 1 30); do docker info >/dev/null 2>&1 && break; echo "waiting for docker daemon ($i/30)..."; sleep 2; done
      docker info >/dev/null 2>&1 || { echo "docker daemon did not become ready"; exit 1; }
  script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
    # molecule-test: the container roles run inside
    - docker build -t "${CI_REGISTRY_IMAGE}/molecule-test:${CI_COMMIT_SHORT_SHA}" docker/molecule-test/
    - docker push "${CI_REGISTRY_IMAGE}/molecule-test:${CI_COMMIT_SHORT_SHA}"
    # molecule-ci: the GitLab CI runner image (docker-cli + pip deps + collections)
    - docker build -t "${CI_REGISTRY_IMAGE}/molecule-ci:${CI_COMMIT_SHORT_SHA}" -f docker/molecule-ci/Dockerfile .
    - docker push "${CI_REGISTRY_IMAGE}/molecule-ci:${CI_COMMIT_SHORT_SHA}"
    # Capture the immutable digest of the CI runner image and emit it as a
    # dotenv var. The .molecule-base `image:` reference consumes MOLECULE_CI_IMAGE
    # so a same-pipeline rebuild can't be raced by anyone retagging :latest
    # mid-flight. The molecule-TEST image needs no digest dotenv: the molecule
    # jobs pull it by its immutable `:${CI_COMMIT_SHORT_SHA}` tag (see
    # .molecule-base before_script), which already pins the test target per
    # pipeline.
    - MOLECULE_CI_DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' "${CI_REGISTRY_IMAGE}/molecule-ci:${CI_COMMIT_SHORT_SHA}")
    - echo "MOLECULE_CI_IMAGE=${MOLECULE_CI_DIGEST}" > molecule-images.env
    - cat molecule-images.env
    # Only push :latest on main — MR pipelines use SHA tags to avoid
    # overwriting :latest with unmerged branch images. The digest pin
    # above is the load-bearing reference; :latest is only used as a
    # fallback for pipelines where build doesn't run (Dockerfile/reqs
    # unchanged), and even then it's the previous main's image.
    - |
      if [ "$CI_COMMIT_BRANCH" = "main" ]; then
        docker tag "${CI_REGISTRY_IMAGE}/molecule-test:${CI_COMMIT_SHORT_SHA}" "${CI_REGISTRY_IMAGE}/molecule-test:latest"
        docker push "${CI_REGISTRY_IMAGE}/molecule-test:latest"
        docker tag "${CI_REGISTRY_IMAGE}/molecule-ci:${CI_COMMIT_SHORT_SHA}" "${CI_REGISTRY_IMAGE}/molecule-ci:latest"
        docker push "${CI_REGISTRY_IMAGE}/molecule-ci:latest"
      fi
  artifacts:
    reports:
      dotenv: molecule-images.env
    expire_in: 7 days
  rules:
    # full-test schedule canary: rebuild the images so external deps baked in
    # at build time (apt, pip, ansible-galaxy) are exercised, not just the
    # stale :latest image's runtime deps. Must precede the schedule
    # `when: never` rule — GitLab rules are first-match-wins.
    - if: '$CI_PIPELINE_SOURCE == "schedule" && $SCHEDULE_TYPE == "full-test"'
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-build-molecule, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-build-molecule, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true

# ============================================================================
# LINT STAGE - Code quality checks
# ============================================================================

shellcheck:
  needs: []
  stage: lint
  # Pinned (not :stable) so lint results are reproducible — a moving tag could
  # silently change shellcheck behavior between runs.
  image: koalaman/shellcheck-alpine:v0.10.0
  script:
    - echo "Running ShellCheck..."
    - shellcheck --severity=warning --exclude=SC1091,SC2034 scripts/*.sh
    - |
      # Also lint shell scripts shipped in Ansible roles.
      # `find ... | while ...` would mask failures: the pipeline's exit code is
      # the while loop's, i.e. the rc of its LAST iteration — a shellcheck
      # failure on any earlier file goes green. `-exec ... {} +` returns
      # non-zero if shellcheck fails on ANY file, so the job fails correctly.
      find ansible/roles -name '*.sh' -type f \
        -exec shellcheck --severity=warning --exclude=SC1091,SC2034 {} +
    - |
      # Lint shell-script Jinja templates (*.sh.j2) shipped in roles (incl. the
      # boot-time crypto-unlock units). We can't render real values, so
      # neutralize Jinja first. The {# #} comment-RANGE delete eats bash
      # ${#arr[@]} and corrupts the body, so {% raw %}-wrapped scripts (literal
      # bash) instead delete only FULL-LINE {# ... #} comments (anchored, can't
      # touch a mid-line ${#...}); plain templates keep the full neutralizer.
      # CONSTRAINT: a multi-line/inline {# #} in the out-of-raw header of a raw-
      # wrapped template isn't stripped — keep header comments to single lines.
      #
      # Accumulate rc (not `find | while`, whose exit status is only the last
      # iteration's) via a list file — not a pipe or process substitution, since
      # this job's shell is ash/sh — so the loop runs in the current shell and
      # rc survives. `rc=1` on any failure + the final `exit $rc` fails the job.
      rendered=$(mktemp)
      tmpl_list=$(mktemp)
      rc=0
      find ansible/roles -name '*.sh.j2' -type f > "$tmpl_list"
      while read -r tmpl; do
        echo "Checking: $tmpl"
        if grep -Eq '^[[:space:]]*\{%-?[[:space:]]*raw[[:space:]]*-?%\}[[:space:]]*$' "$tmpl"; then
          sed -E \
            -e '/^[[:space:]]*\{#.*#\}[[:space:]]*$/d' \
            -e '/\{%.*%\}/d' \
            -e 's/\{\{"\{"\}\}/{/g' \
            -e 's/\{\{"\}"\}\}/}/g' \
            -e 's/\{\{[^}]*\}\}/JINJA/g' "$tmpl" > "$rendered"
        else
          sed -E \
            -e '/\{#/,/#\}/d' \
            -e '/\{%.*%\}/d' \
            -e 's/\{\{"\{"\}\}/{/g' \
            -e 's/\{\{"\}"\}\}/}/g' \
            -e 's/\{\{[^}]*\}\}/JINJA/g' "$tmpl" > "$rendered"
        fi
        shellcheck --severity=warning --exclude=SC1091,SC2034 "$rendered" || rc=1
      done < "$tmpl_list"
      rm -f "$rendered" "$tmpl_list"
      [ "$rc" -eq 0 ] || { echo "ShellCheck failed on one or more *.sh.j2 templates"; exit "$rc"; }
    - echo "ShellCheck passed"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-shellcheck, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-shellcheck, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

# Catches role changes that aren't mapped to any deploy-* job. Fails
# loudly with a list of unmapped roles + the two ways to resolve. See
# scripts/check-deploy-coverage.sh for the gate logic and the
# INTENTIONALLY_UNMAPPED_ROLES list.
deploy-coverage-check:
  needs: []
  stage: lint
  # python:3.11-slim (not alpine): apk installs from dl-cdn.alpinelinux.org
  # returned DNS lookup errors mid-pipeline. The Debian image ships bash +
  # coreutils + python3, so before_script only needs apt-get git + pip
  # PyYAML — no Alpine CDN dependency.
  image: python:3.11-slim
  variables:
    # The script `git merge-base --is-ancestor BASE_REF HEAD` guard
    # rejects unrelated histories — which a default depth-20 GitLab
    # CI clone surfaces as a false positive whenever the MR base is
    # >20 commits behind HEAD. GIT_DEPTH: 0 forces a full clone so
    # the merge-base is always reachable.
    GIT_DEPTH: 0
  before_script:
    # python3 + PyYAML: scripts/check-deploy-coverage.sh parses
    # .gitlab-ci.yml as YAML to extract deploy-job rules (scoped to
    # name=deploy-* AND stage=deploy). Plain `grep -oE` would match
    # paths inside lint/test job rules and report false coverage.
    - apt-get update -qq && apt-get install -y --no-install-recommends git
    - pip install --no-cache-dir --quiet "PyYAML==${PYYAML_VERSION}"
  script:
    - bash scripts/check-deploy-coverage.sh
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-deploy-coverage, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-deploy-coverage, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true

# Fails if a molecule scenario (ansible/roles/*/molecule/*/) or an integration
# test (ansible/integration-tests/*/) exists on disk with no corresponding
# entry in the molecule-tests / integration-tests parallel:matrix. Prevents a
# new role's tests from silently never running in CI. See
# scripts/check-molecule-matrix-coverage.sh.
molecule-matrix-coverage-check:
  needs: []
  stage: lint
  image: python:3.11-slim
  before_script:
    - pip install --no-cache-dir --quiet "PyYAML==${PYYAML_VERSION}"
  script:
    - bash scripts/check-molecule-matrix-coverage.sh
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-molecule-matrix, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-molecule-matrix, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

# deploy-verify hardcodes FLUX_VERSION + tarball sha256 as a pin pair
# (supply-chain), with a runtime guard against versions-configmap. But
# deploy-verify only runs on main, so a flux_version bump that forgets
# the CI pin would surface AFTER merge. This job runs the same drift
# check in MR pipelines so the incomplete bump fails before merge.
flux-version-pin-check:
  needs: []
  stage: lint
  image: python:3.11-slim
  script:
    - |
      set -eu
      PIN=$(sed -n 's/^ *FLUX_VERSION="\([0-9.]*\)"$/\1/p' .gitlab-ci.yml | head -1)
      CM=$(sed -n 's/^ *flux_version: *//p' kubernetes/infrastructure/sources/versions-configmap.yaml | head -1)
      echo "deploy-verify FLUX_VERSION pin: ${PIN:-<not found>} / versions-configmap flux_version: ${CM:-<not found>}"
      [ -n "$PIN" ] || { echo "Could not extract FLUX_VERSION pin from .gitlab-ci.yml"; exit 1; }
      [ -n "$CM" ] || { echo "Could not extract flux_version from versions-configmap.yaml"; exit 1; }
      [ "$PIN" = "$CM" ] || { echo "FLUX_VERSION pin (${PIN}) drifted from versions-configmap flux_version (${CM}) — bump the deploy-verify version + sha256 pair in .gitlab-ci.yml"; exit 1; }
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-flux-version-pin, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-flux-version-pin, changes]

yaml-lint:
  needs: []
  stage: lint
  # `-d relaxed` is a baseline syntax check across the whole tree. Strict
  # enforcement of ansible/.yamllint (160-char line-length, truthy, octal)
  # is owned by the ansible-lint job, which loads that config from `cd
  # ansible` and honors the repo's `# noqa yaml[...]` exemptions — raw
  # yamllint cannot, so don't point this job at ansible/.yamllint.
  before_script:
    # Keep the yamllint pin in step with requirements.txt (molecule-ci image).
    - pip install --quiet yamllint==1.38.0
  script:
    - yamllint -d relaxed ansible/
    - yamllint -d relaxed kubernetes/
    - yamllint -d relaxed .gitlab-ci.yml
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-yaml-lint, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-yaml-lint, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

ansible-lint:
  needs: []
  stage: lint
  extends: .ansible-base
  script:
    - cd ansible
    - |
      export SSH_PUBLIC_KEY="ssh-ed25519 AAAA_PLACEHOLDER_FOR_CI_SYNTAX_CHECK"
      export SMTP_RELAY_USER="ci-placeholder"
      export SMTP_RELAY_PASSWORD="ci-placeholder"
      for playbook in playbooks/*.yml playbooks/*/*.yml; do
        if [ -f "$playbook" ]; then
          # Skip task include files (convention: prefix with underscore)
          # POSIX-compatible pattern match (works in sh, not just bash)
          case "$(basename "$playbook")" in
            _*)
              echo "Skipping task include: $playbook"
              continue
              ;;
          esac
          echo "Checking syntax: $playbook"
          ansible-playbook --syntax-check -i inventories/prod "$playbook"
        fi
      done
    - ansible-lint playbooks/ roles/
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-ansible, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-ansible, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

terraform-fmt:
  needs: []
  stage: lint
  image:
    name: hashicorp/terraform:1.15
    entrypoint: [""]
  script:
    # Cover every module under terraform/ (cloudflare + tailscale), not just one.
    - terraform fmt -check -recursive terraform/
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-terraform, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-terraform, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

# ============================================================================
# AI-REVIEW STAGE - AI-powered code review on MRs
# ============================================================================

# PR-Agent by Qodo - AI code review
# Requires: GitLab API Token and OpenAI API Key in 1Password
pr-agent-review:
  stage: ai-review
  needs:
    # Wait for lint + security only — no need to block on slow test jobs.
    # AI review benefits from knowing the code is lint-clean and secret-safe
    # but doesn't depend on molecule/integration test results.
    - job: shellcheck
      optional: true
    - job: yaml-lint
      optional: true
    - job: ansible-lint
      optional: true
    - job: terraform-fmt
      optional: true
    - job: flux-versions-sync
      optional: true
    - job: flux-lint
      optional: true
    - job: secret_detection
  # Pinned by digest to prevent supply-chain surprises on a job that handles
  # both a GitLab PAT and an OpenAI key. Bump alongside upstream releases.
  image: codiumai/pr-agent:0.34@sha256:1c1743e93bf6627cd14528cd4defe82465f17860e8f20e6c0ac6558971f61c61
  variables:
    CONFIG__GIT_PROVIDER: "gitlab"
    GITLAB__URL: "https://git.ericsweiss.com"
    # CONFIG__MODEL: gpt-5.5 — OpenAI's latest. There is no gpt-5.5-codex (the
    # codex line tops out at gpt-5.3-codex); gpt-5.5 supports xhigh reasoning.
    # See: https://qodo-merge-docs.qodo.ai/usage-guide/changing_a_model/
    CONFIG__MODEL: "gpt-5.5"
    # Use (most of) gpt-5.5's ~1M context for deep review. Its window is 1.05M
    # total = ~922k input + 128k output; get_max_tokens() returns TOTAL context
    # and does NOT reserve output, so cap usage at 900k to stay under the input
    # limit (with headroom for the system prompt). This is the operational cap
    # that get_max_tokens min()s against.
    CONFIG__MAX_MODEL_TOKENS: "900000"
    # gpt-5.5 shipped after pr-agent 0.34, so it is absent from pr-agent's
    # MAX_TOKENS table — declare the model's context window explicitly or
    # get_max_tokens() errors out ("Model gpt-5.5 is not defined in MAX_TOKENS").
    # Set to the full ~1.05M window; MAX_MODEL_TOKENS above is the real cap.
    # (Reasoning + temperature-omission still apply via the gpt-5* prefix check.)
    CONFIG__CUSTOM_MODEL_MAX_TOKENS: "1050000"
    # 'high', not 'xhigh': the litellm bundled in pr-agent 0.34 only allows
    # reasoning_effort='xhigh' for gpt-5.1-codex-max and gpt-5.2 — for gpt-5.5 it
    # raises UnsupportedParamsError, which makes pr-agent fall back to o4-mini.
    # 'high' is litellm's max for gpt-5.5; revisit if pr-agent ships a newer
    # litellm that knows gpt-5.5 supports xhigh.
    CONFIG__REASONING_EFFORT: "high"
    # Per-LLM-call timeout (litellm). The 120s default timed out on large diffs
    # with xhigh reasoning (litellm.Timeout in the logs); 600s gives reasoning
    # calls headroom. allow_failure + the 45m job timeout bound the worst case.
    CONFIG__AI_TIMEOUT: "600"
    PR_REVIEWER__NUM_CODE_SUGGESTIONS: "6"
    PR_REVIEWER__EXTRA_INSTRUCTIONS: "Be thorough. Review every file. Flag all bugs, security issues, and documentation drift."
    PR_CODE_SUGGESTIONS__NUM_CODE_SUGGESTIONS: "6"
  before_script:
    - !reference [.install-1password, before_script]
  script:
    - |
      # Get secrets from 1Password
      export GITLAB__PERSONAL_ACCESS_TOKEN=$(op read "op://Homelab/GitLab API Token/credential")
      export OPENAI__KEY=$(op read "op://Homelab/OpenAI API Key/api-key")

      echo "Running AI code review on MR !${CI_MERGE_REQUEST_IID}..."
      echo "Model: gpt-5.5 (high reasoning, 900k context, 600s call timeout)"

      # Run full review
      python -m pr_agent.cli \
        --pr_url="https://git.ericsweiss.com/eric/weisssrv/-/merge_requests/${CI_MERGE_REQUEST_IID}" \
        review

      # Suggest improvements
      python -m pr_agent.cli \
        --pr_url="https://git.ericsweiss.com/eric/weisssrv/-/merge_requests/${CI_MERGE_REQUEST_IID}" \
        improve

      echo "AI review complete"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    # OPERATIONAL INVARIANT: the && $OP_SERVICE_ACCOUNT_TOKEN guard means this
    # job is never CREATED when the token is unset (e.g. fork MRs). An absent
    # AI review is therefore expected, not a failure — a missing pr-agent-review
    # on such an MR is not a regression. validation-gate does NOT need this job
    # (AI review is advisory), so its absence never blocks deploys.
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $OP_SERVICE_ACCOUNT_TOKEN'
  allow_failure: true  # Don't block MRs on AI review
  # 45m (> the 30m default): xhigh reasoning + the 600s per-call timeout across
  # the review + improve passes can run long; allow_failure means it never blocks.
  timeout: 45m

# ============================================================================
# VALIDATE STAGE - Schema and configuration validation
# ============================================================================

terraform-validate:
  needs: []
  stage: validate
  image:
    name: hashicorp/terraform:1.15
    entrypoint: [""]
  script:
    # Validate every module under terraform/ (cloudflare + tailscale). Each needs
    # its own init (different providers); -backend=false skips state/creds.
    - |
      set -eo pipefail
      for mod in terraform/*/; do
        [ -f "${mod}versions.tf" ] || continue
        echo "=== terraform validate ${mod} ==="
        ( cd "$mod" && terraform init -backend=false && terraform validate )
      done
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-terraform, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-terraform, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

terraform-plan:
  extends: .terraform-http-backend
  needs: []
  stage: validate
  image:
    name: hashicorp/terraform:1.15
    entrypoint: [""]
  before_script:
    - !reference [.install-1password-alpine, before_script]
  script:
    - cd terraform/cloudflare
    # -lockfile=readonly: CI must not rewrite .terraform.lock.hcl (provider
    # hashes are committed; a CI-side change would mask supply-chain drift).
    - terraform init -lockfile=readonly
    - |
      export TF_VAR_cloudflare_api_token=$(op read "op://Homelab/Cloudflare Terraform Token/credential")
      export TF_VAR_cloudflare_account_id=$(op read "op://Homelab/Cloudflare Terraform Token/username")
      terraform plan -input=false -no-color -lock-timeout=120s -out=tfplan
    # Generate plan report for MR widget (shows resource change counts in merge requests)
    - terraform show -json tfplan | jq -r '
        ([.resource_changes[]?.change.actions?]|flatten)|
        {"create":(map(select(.=="create"))|length),
         "update":(map(select(.=="update"))|length),
         "delete":(map(select(.=="delete"))|length)}
      ' > tfplan.json
  artifacts:
    # The binary plan embeds rendered resource attributes; restrict download to
    # developer+ (default is any project member). deploy-terraform still consumes
    # it via needs:artifacts — access only gates UI/API download, not job needs.
    access: developer
    paths:
      - terraform/cloudflare/tfplan
    reports:
      terraform: terraform/cloudflare/tfplan.json
    # 1 day (not 1 hour): deploy-terraform consumes this tfplan in a later stage;
    # a slow/retried pipeline could otherwise have the plan GC'd before apply.
    expire_in: 1 day
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    # OPERATIONAL INVARIANT: the && $OP_SERVICE_ACCOUNT_TOKEN guard means a
    # terraform/ change on a tokenless pipeline (e.g. fork MR) produces NO plan
    # — the MR widget will simply lack a Terraform report. That's an expected
    # skip, not a clean plan. validation-gate needs this job optionally, so a
    # token-gated skip can't silently mask drift on a real (token-bearing) run.
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes: !reference [.paths-terraform, changes]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes: !reference [.paths-terraform, changes]

flux-versions-sync:
  needs: []
  # Fails if kubernetes/infrastructure/sources/versions-configmap.yaml is out
  # of sync with ansible/inventories/prod/group_vars/all.yml. Run
  # `task flux:sync-versions` locally to regenerate and commit.
  stage: lint
  image: python:3.11-slim
  before_script:
    - pip install --quiet "pyyaml==${PYYAML_VERSION}"
  script:
    # Compare committed ConfigMap against a freshly-generated one. The
    # generator writes in-place, so we save + restore the committed file
    # to keep the workspace clean for other jobs (e.g., flux-lint).
    - |
      set -eo pipefail
      TARGET=kubernetes/infrastructure/sources/versions-configmap.yaml
      cp "$TARGET" /tmp/committed.yaml
      python3 scripts/generate-versions-configmap.py
      cp "$TARGET" /tmp/generated.yaml
      cp /tmp/committed.yaml "$TARGET"   # restore workspace
      if ! diff -q /tmp/committed.yaml /tmp/generated.yaml >/dev/null 2>&1; then
        echo "ERROR: versions-configmap.yaml is out of sync with all.yml"
        echo "Run: task flux:sync-versions and commit the result."
        diff -u /tmp/committed.yaml /tmp/generated.yaml || true
        exit 1
      fi
      echo "versions-configmap.yaml is in sync with all.yml"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-flux-versions-sync, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-flux-versions-sync, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

hosts-env-sync:
  needs: []
  # Fails if scripts/hosts.env is out of sync with the host roster in
  # ansible/inventories/prod/hosts.yml. Run `task hosts:sync` locally to
  # regenerate and commit. Mirrors flux-versions-sync (same drift-guard pattern).
  stage: lint
  image: python:3.11-slim
  before_script:
    - pip install --quiet "pyyaml==${PYYAML_VERSION}"
  script:
    # Compare the committed file against a freshly-generated one. The generator
    # writes in place, so save + restore to keep the workspace clean for the
    # keyscan (which sources hosts.env) and other jobs.
    - |
      set -eo pipefail
      TARGET=scripts/hosts.env
      cp "$TARGET" /tmp/committed.env
      python3 scripts/generate-hosts-env.py
      cp "$TARGET" /tmp/generated.env
      cp /tmp/committed.env "$TARGET"   # restore workspace
      if ! diff -q /tmp/committed.env /tmp/generated.env >/dev/null 2>&1; then
        echo "ERROR: scripts/hosts.env is out of sync with hosts.yml"
        echo "Run: task hosts:sync and commit the result."
        diff -u /tmp/committed.env /tmp/generated.env || true
        exit 1
      fi
      echo "scripts/hosts.env is in sync with hosts.yml"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-hosts-env-sync, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-hosts-env-sync, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

python-tests:
  # Runs pytest against scripts/. test_*.py live next to the scripts they
  # exercise (version-update tooling, the ConfigMap generator, the HPA/VPA
  # invariant guard).
  stage: test
  needs: []  # run in parallel with lint stage
  image: python:3.11-slim
  before_script:
    # git is required by test_check_deploy_coverage.py (it builds throwaway git
    # repos to exercise check-deploy-coverage.sh); the slim image omits it.
    - apt-get update -qq && apt-get install -y -qq --no-install-recommends git
    - pip install --quiet "pytest==${PYTEST_VERSION}" "pyyaml==${PYYAML_VERSION}"
  script:
    - pytest scripts/ -v --junitxml=report.xml
  artifacts:
    when: always
    reports:
      junit: report.xml
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    # The pytest suite validates real tree files beyond scripts/: test_adguard_api_body.py
    # asserts on ansible/roles/adguard_home/tasks/api_base_config.yml, and
    # test_check_molecule_matrix_coverage.py smoke-tests the live .gitlab-ci.yml matrix +
    # molecule/integration dirs. Those paths must be in `changes:` or an MR that edits
    # only them would skip the tests that guard them.
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-python-tests, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-python-tests, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

flux-lint:
  needs: []
  # Renders every top-level Flux Kustomization, substitutes postBuild vars
  # from kubernetes/infrastructure/sources/versions-configmap.yaml, verifies
  # zero unsubstituted ${var} placeholders remain, then validates with
  # kubeconform. Also schema-validates the value-heavy HelmReleases'
  # .spec.values via `helm template` (kustomize build emits HelmReleases
  # verbatim, so a chart values-key typo would otherwise be unchecked).
  stage: lint
  image: python:3.11-slim
  before_script:
    - apt-get update -qq && apt-get install -y -qq curl gettext-base
    - pip install --quiet "pyyaml==${PYYAML_VERSION}"
    - |
      KUBECONFORM_SHA256="95f14e87aa28c09d5941f11bd024c1d02fdc0303ccaa23f61cef67bc92619d73"
      curl -fsSL -o /tmp/kubeconform.tar.gz \
        https://github.com/yannh/kubeconform/releases/download/v0.6.7/kubeconform-linux-amd64.tar.gz
      echo "${KUBECONFORM_SHA256}  /tmp/kubeconform.tar.gz" | sha256sum -c -
      tar xzf /tmp/kubeconform.tar.gz -C /usr/local/bin kubeconform
      curl -fsSL "https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize%2Fv${KUSTOMIZE_VERSION}/kustomize_v${KUSTOMIZE_VERSION}_linux_amd64.tar.gz" -o /tmp/kustomize.tar.gz
      echo "${KUSTOMIZE_SHA256}  /tmp/kustomize.tar.gz" | sha256sum -c -
      tar xzf /tmp/kustomize.tar.gz -C /usr/local/bin kustomize
      # helm: schema-validates HelmRelease .spec.values (scripts/validate-helm-values.py).
      # Tarball unpacks to linux-amd64/helm (unlike kubeconform/kustomize bare binaries).
      HELM_SHA256="f8180838c23d7c7d797b208861fecb591d9ce1690d8704ed1e4cb8e2add966c1"
      curl -fsSL -o /tmp/helm.tar.gz https://get.helm.sh/helm-v3.18.4-linux-amd64.tar.gz
      echo "${HELM_SHA256}  /tmp/helm.tar.gz" | sha256sum -c -
      tar xzf /tmp/helm.tar.gz -C /usr/local/bin --strip-components=1 linux-amd64/helm
      kubeconform -v && kustomize version && helm version --short && envsubst --version | head -1
  script:
    - |
      set -eo pipefail
      FAILED=0
      # Extract cluster-versions ConfigMap keys and export as env so envsubst
      # can substitute ${var} placeholders in rendered manifests. This mirrors
      # what Flux's kustomize-controller does at reconcile time (postBuild).
      # Shared with Taskfile flux:lint via scripts/flux-render.sh so the two
      # paths can't drift.
      VERSIONS_CONFIGMAP=kubernetes/infrastructure/sources/versions-configmap.yaml
      VARS=$(bash scripts/flux-render.sh export-versions "$VERSIONS_CONFIGMAP") \
        || { echo "ERROR: failed to extract version keys from $VERSIONS_CONFIGMAP"; exit 1; }
      eval "$VARS"

      # Accumulate the full rendered corpus so the HPA/VPA invariant check
      # (below) sees standalone HPAs + VPAs across every namespace at once.
      # Mirrors the Taskfile flux:lint RENDER_ALL pattern.
      RENDER_ALL=$(mktemp)
      trap 'rm -f "$RENDER_ALL"' EXIT

      # kubeconform schema version derived from k3s_version (shared helper) so
      # local lint + CI validate against the same MAJOR.MINOR.0 API schemas.
      K8S_VER=$(bash scripts/flux-render.sh k8s-version "$VERSIONS_CONFIGMAP")
      echo "kubeconform -kubernetes-version: $K8S_VER (from versions-configmap k3s_version)"
      # Iterate every Flux Kustomization under clusters/weisssrv/ and validate
      # the target path it points at. This picks up new top-level Kustomizations
      # automatically without CI edits.
      for ks in kubernetes/clusters/weisssrv/*.yaml; do
        [ -f "$ks" ] || continue
        NAME=$(basename "$ks" .yaml)
        [ "$NAME" = "kustomization" ] && continue
        if ! SRCPATH=$(python3 -c "
      import yaml, sys
      with open('$ks') as f:
          doc = yaml.safe_load(f)
      print(doc.get('spec', {}).get('path', '') or '')
      "); then
          echo "ERROR: failed to parse $ks"
          FAILED=1
          continue
        fi
        [ -z "$SRCPATH" ] && continue
        SRCPATH="${SRCPATH#./}"
        echo "=== Building + validating $ks (path $SRCPATH) ==="
        if ! RAW=$(kustomize build "$SRCPATH" 2>&1); then
          echo "ERROR: kustomize build failed for $SRCPATH:"
          echo "$RAW"
          FAILED=1
          continue
        fi
        # Check every ${var} reference in the pre-substitution output against
        # the exported ConfigMap keys. envsubst renders unknown vars as empty
        # string (so post-substitution grep wouldn't catch them) — Flux has
        # the same silent-empty default behavior. Our lint is intentionally
        # stricter so typos in version keys fail CI.
        MISSING=""
        for ref in $(echo "$RAW" | grep -oE '\$\{[A-Za-z_][A-Za-z0-9_]*\}' | sort -u); do
          varname=${ref#\$\{}
          varname=${varname%\}}
          if [ -z "${!varname+x}" ]; then
            MISSING="$MISSING $varname"
          fi
        done
        MISSING=$(echo "$MISSING" | tr ' ' '\n' | grep -v '^$' | sort -u || true)
        if [ -n "$MISSING" ]; then
          echo "ERROR: placeholders in $SRCPATH reference keys not in versions-configmap:"
          echo "$MISSING" | sed 's/^/  - /'
          FAILED=1
          continue
        fi
        # printf '%s\n' (NOT echo): rendered manifests carry Grafana dashboard
        # JSON with thousands of literal \n inside block scalars, and echo would
        # expand those into real newlines and corrupt the YAML stream (same
        # rationale as the deploy-verify dry-run block).
        RENDERED=$(printf '%s\n' "$RAW" | envsubst "$FLUX_ENVSUBST_VARS")
        printf '%s\n---\n' "$RENDERED" >> "$RENDER_ALL"
        # KNOWN GAP: -ignore-missing-schemas silently SKIPS any CR whose schema
        # is absent from (or unreachable in) the datreeio CRDs-catalog. Most CRDs
        # we use (IngressRoute, VPA, ExternalSecret, ServiceMonitor, Certificate)
        # are in the catalog, but a CR for a kind not in the catalog is unchecked.
        # Tracked as a follow-up (vendor CRD JSON schemas + a second strict pass).
        if ! printf '%s\n' "$RENDERED" | kubeconform -strict -ignore-missing-schemas \
            -kubernetes-version "$K8S_VER" \
            -schema-location default \
            -schema-location 'https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json' \
            -summary; then
          FAILED=1
        fi
      done

      # HPA/VPA invariant: no workload may have both an HPA and a VPA driving
      # the same resource (docs/33-autoscaling.md). --require-chart-native-vpas
      # also statically asserts the chart-native-HPA workloads
      # (Traefik/authentik/Connect) each have a memory-only VPA in the rendered
      # corpus. MUST match the Taskfile flux:lint invocation — without the flag
      # the gate would silently skip the chart-native assertion.
      echo "=== Checking HPA/VPA invariant ==="
      if ! python3 scripts/check-hpa-vpa-invariant.py --require-chart-native-vpas < "$RENDER_ALL"; then
        FAILED=1
      fi

      # Also build the cluster root to catch malformed top-level kustomization.yaml
      # (the loop above validates target paths, not the cluster root itself).
      echo "=== Building cluster root kubernetes/clusters/weisssrv/ ==="
      if ! kustomize build kubernetes/clusters/weisssrv >/dev/null; then
        echo "ERROR: cluster root kustomization failed to build"
        FAILED=1
      fi

      # Schema-validate the value-heavy HelmReleases' .spec.values via
      # `helm template` — kustomize build above emits HelmReleases verbatim, so
      # a typo in a chart values key (.spec.values is a free-form object in the
      # HelmRelease CRD) would otherwise be unchecked. Needs network (helm repo
      # add/update). Covers traefik (values.schema.json catches key typos),
      # kube-prometheus-stack, authentik. MUST match the Taskfile flux:lint call.
      echo "=== Schema-validating HelmRelease values blocks (helm template) ==="
      if ! python3 scripts/validate-helm-values.py --kubeconform; then
        FAILED=1
      fi

      exit $FAILED
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-flux-lint, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-flux-lint, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

# Validate the kube-prometheus-stack alert rules (PromQL in HelmRelease values)
# and the Alertmanager config (ExternalSecret template) that kubeconform /
# flux-lint can't parse. Without this, a bad expr or malformed route only
# surfaces post-merge when the prometheus-operator admission webhook rejects the
# rendered CR (failing the whole HelmRelease upgrade). See docs/33.
prometheus-config-lint:
  needs: []
  stage: lint
  image: python:3.11-slim
  before_script:
    - apt-get update -qq && apt-get install -y -qq curl
    - pip install --quiet "pyyaml==${PYYAML_VERSION}"
    - |
      set -eo pipefail
      # promtool (prometheus) + amtool (alertmanager), pinned + sha256 like every
      # other downloaded binary in this pipeline. set -eo pipefail so a failing
      # sha256sum -c aborts the block — without it only the last line's status
      # counts and the integrity pin would be decorative.
      PROM_VERSION="3.6.0"
      curl -fsSL "https://github.com/prometheus/prometheus/releases/download/v${PROM_VERSION}/prometheus-${PROM_VERSION}.linux-amd64.tar.gz" -o /tmp/prom.tar.gz
      echo "2002ef4a55a64161affccd2786c7081d4e3b3a8d08786a98b3bb110971414916  /tmp/prom.tar.gz" | sha256sum -c -
      tar xzf /tmp/prom.tar.gz -C /usr/local/bin --strip-components=1 "prometheus-${PROM_VERSION}.linux-amd64/promtool"
      AM_VERSION="0.28.1"
      curl -fsSL "https://github.com/prometheus/alertmanager/releases/download/v${AM_VERSION}/alertmanager-${AM_VERSION}.linux-amd64.tar.gz" -o /tmp/am.tar.gz
      echo "5ac7ab5e4b8ee5ce4d8fb0988f9cb275efcc3f181b4b408179fafee121693311  /tmp/am.tar.gz" | sha256sum -c -
      tar xzf /tmp/am.tar.gz -C /usr/local/bin --strip-components=1 "alertmanager-${AM_VERSION}.linux-amd64/amtool"
      promtool --version && amtool --version
  script:
    - bash scripts/lint-prometheus-config.sh
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-prometheus-config, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-prometheus-config, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

# ============================================================================
# TEST STAGE - Unit and integration tests
# ============================================================================

# NOTE: Molecule/integration tests require Docker-in-Docker, which needs a privileged
# runner. All weisssrv jobs (including these) run on the infrastructure runner
# (gitlab-runner-privileged Helm release, tags: infrastructure) which has privileged=true.
# The runner is Flux-managed: edit kubernetes/apps/gitlab-runner-privileged/release.yaml
# and push; Flux reconciles within ~1 min (or run `task flux:reconcile` to force).

# Molecule unit tests — one parallel job per role/scenario. Each role with a
# molecule/ directory gets its own CI job via parallel:matrix.
.molecule-base:
  stage: test
  # Backstop for infra-class scheduling failures (a pod that briefly can't be
  # placed during transition churn surfaces as runner_system_failure — wide
  # molecule fan-outs can exhaust schedulable CPU/memory and time a job pod
  # out Pending before any test runs). The primary fix is right-sizing the
  # job resources + quota (gitlab-runner-privileged) so these are rare;
  # deliberately NOT retrying script_failure (a failing test must not be
  # masked — transient in-container start storms are handled by the in-job
  # destroy+jitter loop in each test job) or stuck_or_timeout_failure (a
  # genuinely hung test should fail fast, not burn another full timeout).
  retry:
    max: 2
    when:
      - runner_system_failure
      - scheduler_failure
  needs:
    - job: build-molecule-images
      optional: true
      # When it runs (Dockerfile/requirements changed this pipeline), the build
      # job emits a dotenv pinning MOLECULE_CI_IMAGE to the image digest, which
      # overrides the :latest fallback below; artifacts:true consumes it.
      artifacts: true
  # Pre-built CI runner image (docker-cli + molecule + ansible + collections
  # baked in) — saves ~2 min of apt/pip/galaxy per job vs python:3.11.
  # `image:` only supports single-var expansion ($VAR), not bash defaults, so
  # the :latest fallback lives in the variables block below; the build job's
  # dotenv digest pin (above) overrides it on the hot path (anti-retag race).
  image: $MOLECULE_CI_IMAGE
  services:
    - docker:24.0-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
    ANSIBLE_FORCE_COLOR: "1"
    PY_COLORS: "1"
    ANSIBLE_CALLBACKS_ENABLED: junit
    JUNIT_OUTPUT_DIR: "$CI_PROJECT_DIR/junit"
    MOLECULE_IMAGE: "${CI_REGISTRY_IMAGE}/molecule-test"
    # Fallback when build-molecule-images is skipped this pipeline; its
    # dotenv artifact (when present) overrides this with the digest pin.
    MOLECULE_CI_IMAGE: "${CI_REGISTRY_IMAGE}/molecule-ci:latest"
  before_script:
    # Wait for DinD sidecar — with the pre-built CI image, the script
    # starts before DinD is ready (no apt-get delay to absorb startup).
    - |
      for i in $(seq 1 30); do
        docker info >/dev/null 2>&1 && break
        echo "Waiting for DinD... ($i)"
        sleep 1
      done
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
    - |
      if docker pull "${MOLECULE_IMAGE}:${CI_COMMIT_SHORT_SHA}" 2>/dev/null; then
        docker tag "${MOLECULE_IMAGE}:${CI_COMMIT_SHORT_SHA}" "${MOLECULE_IMAGE}:latest"
      else
        docker pull "${MOLECULE_IMAGE}:latest"
      fi
    - mkdir -p "$CI_PROJECT_DIR/junit"
  artifacts:
    when: always
    reports:
      junit: junit/*.xml
  timeout: 20m

molecule-tests:
  extends: .molecule-base
  script:
    - cd "ansible/roles/$ROLE"
    # In-job retry with destroy + jitter. Under full pipeline fan-out,
    # simultaneous systemd-container starts on a shared node race cgroup
    # setup and die at prepare ("Container X is not running" — pipelines
    # 360/361/365, several roles, always the same second). Job-level
    # retry re-rolls the same dice (all attempts land in the same
    # storm); destroy + a randomized 20-65s sleep de-conflicts the
    # restart instead. $RANDOM is per-shell and the runner pod start
    # times differ, so the jitter genuinely spreads.
    # The default scenarios inherit shared config from ansible/molecule/base.yml
    # via -c; the self-contained non-default scenarios (k3s/agent,
    # resolv_conf/empty-search) keep their full molecule.yml and get no -c.
    - |
      base_opt=""
      [ "$SCENARIO" = "default" ] && base_opt="-c $CI_PROJECT_DIR/ansible/molecule/base.yml"
      # max=4 (was 3): under a full ~55-job fan-out the container-start storm
      # can outlast 3 attempts and exhaust; a 4th widely-jittered attempt
      # usually catches a lull. Only flaky jobs pay it (a healthy job passes on
      # attempt 1), and 4 * (test + <=65s jitter) still fits the job timeouts.
      attempt=1; max=4
      until molecule $base_opt test -s "$SCENARIO"; do
        rc=$?
        attempt=$((attempt+1))
        if [ "$attempt" -gt "$max" ]; then exit "$rc"; fi
        echo "molecule attempt $((attempt-1)) failed (rc=$rc); destroying + retrying ($attempt/$max)"
        molecule $base_opt destroy -s "$SCENARIO" || true
        sleep $(( (RANDOM % 46) + 20 ))
      done
  parallel:
    matrix:
      # Each role/scenario combo gets its own parallel job. Roles with a
      # single scenario use "default". k3s has two scenarios (default +
      # agent) that run as separate jobs to halve wall time.
      - ROLE: acme_certs
        SCENARIO: default
      - ROLE: alloy_host
        SCENARIO: default
      - ROLE: adguard_home
        SCENARIO: default
      - ROLE: adguard_sync
        SCENARIO: default
      - ROLE: base
        SCENARIO: default
      - ROLE: gitlab
        SCENARIO: default
      # home_assistant: contract test for the two Jinja2 templates
      # (configuration.yaml.j2, secrets.yaml.j2). The role's scp/ssh
      # path against the live HAOS appliance can't be exercised in a
      # container — HAOS isn't a Linux distro you can run in Docker —
      # so the scenario flips home_assistant_render_only=true to skip
      # the deploy half and asserts on the rendered output.
      - ROLE: home_assistant
        SCENARIO: default
      - ROLE: k3s
        SCENARIO: default
      - ROLE: k3s
        SCENARIO: agent
      - ROLE: nas_storage
        SCENARIO: default
      - ROLE: nfs_tls
        SCENARIO: default
      - ROLE: nic_tuning
        SCENARIO: default
      - ROLE: plex
        SCENARIO: default
      - ROLE: postfix_null_client
        SCENARIO: default
      - ROLE: qol
        SCENARIO: default
      - ROLE: smtp_relay
        SCENARIO: default
      - ROLE: tailscale
        SCENARIO: default
      - ROLE: unbound
        SCENARIO: default
      # apt_signed_repo is the shared signed-apt-repo pipeline backing
      # alloy_host + gitlab + plex; its scenario hermetically tests both the
      # accept (correct fingerprint) and reject (wrong fingerprint) paths.
      - ROLE: apt_signed_repo
        SCENARIO: default
      # prometheus_exporter is the shared install pipeline backing
      # zfs_exporter + unbound_exporter; its scenario contract-tests both
      # artifact branches (tarball + deb) on the skip path.
      - ROLE: prometheus_exporter
        SCENARIO: default
      - ROLE: zfs_exporter
        SCENARIO: default
      - ROLE: unbound_exporter
        SCENARIO: default
      - ROLE: node_exporter_host
        SCENARIO: default
      - ROLE: resolv_conf
        SCENARIO: default
      - ROLE: resolv_conf
        SCENARIO: empty-search
      # zvol_mount uses loop devices in prepare; works under privileged DinD.
      - ROLE: zvol_mount
        SCENARIO: default
      - ROLE: proxmox_firewall
        SCENARIO: default
      - ROLE: proxmox_vm
        SCENARIO: default
      - ROLE: proxmox_lxc
        SCENARIO: default
      - ROLE: proxmox_ha
        SCENARIO: default
      - ROLE: proxmox_backup
        SCENARIO: default
      # zfs_encryption: validates template rendering, file modes, systemd
      # ordering hooks, and per-pool service enable. Cannot exercise
      # `zfs load-key` (no kernel module in container) or live Connect
      # calls (no fixture endpoint), but covers the deploy contract end
      # to end with mocked inputs.
      - ROLE: zfs_encryption
        SCENARIO: default
  rules:
    # full-test schedule canary (must precede the schedule when:never rule —
    # first-match-wins): catches external-dependency rot (apt/galaxy/upstream
    # installers) without waiting for an unrelated ansible MR to turn red.
    - if: '$CI_PIPELINE_SOURCE == "schedule" && $SCHEDULE_TYPE == "full-test"'
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-molecule, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-molecule, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

integration-tests:
  extends: .molecule-base
  # Multi-container scenarios run 2-3 systemd containers through full
  # role stacks; base-infrastructure alone is ~12m idle and blew the
  # inherited 20m ceiling under pipeline fan-out contention (361).
  timeout: 35m
  script:
    - cd "ansible/integration-tests/$TEST"
    # Same in-job destroy+jitter retry as molecule-tests (see comment
    # there) — multi-container scenarios are the most start-storm-prone.
    - |
      attempt=1; max=4
      until molecule test; do
        rc=$?
        attempt=$((attempt+1))
        if [ "$attempt" -gt "$max" ]; then exit "$rc"; fi
        echo "molecule attempt $((attempt-1)) failed (rc=$rc); destroying + retrying ($attempt/$max)"
        molecule destroy || true
        sleep $(( (RANDOM % 46) + 20 ))
      done
  parallel:
    matrix:
      - TEST:
          - base-infrastructure
          - cert-distribution
          - dns-stack
          - mail-stack
          - storage-stack
  rules:
    # full-test schedule canary — same rationale as molecule-tests.
    - if: '$CI_PIPELINE_SOURCE == "schedule" && $SCHEDULE_TYPE == "full-test"'
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes: !reference [.paths-integration, changes]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: !reference [.paths-integration, changes]
    - if: '$CI_PIPELINE_SOURCE == "web"'

# ============================================================================
# SECURITY STAGE - Secret detection and scanning
# ============================================================================

include:
  - template: Jobs/Secret-Detection.gitlab-ci.yml

secret_detection:
  stage: security
  needs: []  # run in parallel with lint/test stages
  variables:
    SECRET_DETECTION_HISTORIC_SCAN: "false"
    # Custom gitleaks config is loaded via `.gitlab/secret-detection-ruleset.toml`
    # which `[[secrets.passthrough]]`s `.gitleaks.toml`. The previously-used
    # `SECRET_DETECTION_RULESET_PATH` variable is not a documented GitLab
    # variable and was silently ignored. `.gitleaks.toml` carries
    # content-allowlist regexes (`op://`, `[your-...]`, anchored test/fake
    # fixture tokens) that cover the placeholder shapes docs use — `docs/*.md`
    # is scanned normally — plus a path-allowlist for the molecule/
    # integration-test trees only (throwaway PEM fixtures). Trade-off (a real
    # secret committed under those test paths won't be caught here) is
    # documented in `.gitleaks.toml` header.
    # Require modern CPU (gitleaks binary uses POPCNT/SSE4.2 - OptiPlex Core 2 Quads crash with SIGILL)
    # OPERATIONAL INVARIANT: this job is pinned to esweiss.com/cpu=modern nodes.
    # If no modern-CPU node is schedulable the job sits Pending and is NOT a
    # passed scan — a skipped/pending secret_detection must be investigated, not
    # treated as green. validation-gate needs it non-optionally so this can't be
    # silently lost on a main pipeline.
    KUBERNETES_NODE_SELECTOR_CPU: "esweiss.com/cpu=modern"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main"'
    # Run on schedules so anything that snuck in via tags/branch merges that
    # didn't trigger a normal pipeline still gets scanned (security best practice).
    - if: '$CI_PIPELINE_SOURCE == "schedule"'

# ============================================================================
# GATE STAGE - Validation gate for deploy jobs
# ============================================================================
# All deploy jobs depend on this gate (non-optional). The gate lists every
# lint/test/security job as a dependency. GitLab CI `optional: true` semantics
# (https://docs.gitlab.com/ci/yaml/#needsoptional):
#
#   - If the needed job was NOT created (rules excluded it): dependency is ignored
#   - If the needed job IS in the pipeline and SUCCEEDS: dependency satisfied
#   - If the needed job IS in the pipeline and FAILS: the dependent job is BLOCKED
#
# Jobs with `changes:` path filters use optional:true (may not exist in pipeline).
# Jobs that ALWAYS run on main pushes (no path filter) are non-optional.
#
# Net effect: all applicable checks must pass before any deploy job runs.

# Fan-in for the parallel test matrices. molecule-tests (one job per role) and
# integration-tests (one per stack) are `parallel: matrix` jobs, so a `needs:`
# on either expands to ONE entry PER matrix instance (32 + 5 = 37). Listed
# directly in validation-gate alongside its scalar needs the total hit 52, over
# GitLab's hard cap of 50 needs-per-job, so the main pipeline failed to even be
# created ("one job can only need 50 others"). This thin job collapses those 37
# into a single dependency: it needs both matrices (37 < 50) and validation-gate
# needs only this. Gating is unchanged — a failed matrix instance leaves its
# (non-optional-by-default) dependency unmet, this job is skipped, and
# validation-gate (which needs it) is skipped in turn, blocking every deploy job.
# optional:true on each matrix keeps a path-filtered no-op pipeline green.
test-aggregate:
  stage: security
  image: alpine:3.23
  interruptible: true
  needs:
    # artifacts: false — this is a status-only fan-in (script is a bare echo).
    # The matrices extend .molecule-base and emit junit artifacts; without this,
    # GitLab would download them from all 37 instances here for no reason.
    - job: molecule-tests
      optional: true
      artifacts: false
    - job: integration-tests
      optional: true
      artifacts: false
  script:
    - echo "molecule + integration test matrices complete"
  # These rules MUST stay identical to validation-gate's below: it needs this
  # job non-optionally, so if this job is created for a pipeline where that gate
  # is not (or vice versa), pipeline creation fails. Same rules => co-present.
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'

validation-gate:
  stage: gate
  image: alpine:3.23
  needs:
    # -- Path-filtered jobs (may not exist in pipeline) --
    - job: shellcheck
      optional: true
    - job: yaml-lint
      optional: true
    - job: ansible-lint
      optional: true
    - job: terraform-fmt
      optional: true
    - job: terraform-validate
      optional: true
    - job: terraform-plan
      optional: true
    - job: flux-versions-sync
      optional: true
    - job: hosts-env-sync
      optional: true
    - job: flux-lint
      optional: true
    - job: prometheus-config-lint
      optional: true
    - job: flux-version-pin-check
      optional: true
    - job: python-tests
      optional: true
    # molecule-tests + integration-tests are fanned in via test-aggregate (see
    # its comment above) so this gate's expanded needs stay under GitLab's hard
    # 50-per-job cap. test-aggregate carries the SAME rules as this gate (skip-
    # schedule-web + main-only), so the two are always created together. This
    # need is intentionally NON-optional: if that shared-rules invariant is ever
    # broken, a loud pipeline-creation failure is safer than optional's silent
    # skip, which would let deploy run with the test matrices un-gated. Keep the
    # two rule blocks identical (or make this optional) if you change either.
    - job: test-aggregate
    - job: deploy-coverage-check
      optional: true
    - job: molecule-matrix-coverage-check
      optional: true
    # -- Always-run jobs (no path filter, always present on main pushes) --
    - job: secret_detection
  script:
    # Guard: every deploy-* job gates on `$CI_COMMIT_BRANCH == "main" &&
    # $OP_SERVICE_ACCOUNT_TOKEN`. If the token is unset/empty those jobs are
    # silently never created and main goes green having deployed nothing.
    # Fail loudly here (this gate always runs on main, no token guard) so a
    # missing CI/CD variable surfaces as a red pipeline instead of a no-op.
    - |
      if [ -z "$OP_SERVICE_ACCOUNT_TOKEN" ]; then
        echo "ERROR: OP_SERVICE_ACCOUNT_TOKEN is unset/empty on a main pipeline." >&2
        echo "       All deploy-* jobs gate on this token and would be silently skipped," >&2
        echo "       deploying nothing while the pipeline reports success." >&2
        echo "       Set the OP_SERVICE_ACCOUNT_TOKEN CI/CD variable (masked, protected)." >&2
        exit 1
      fi
    - echo "All applicable checks passed - deploy jobs may proceed"
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'

# ============================================================================
# DEPLOY STAGE - Auto-deploy on merge to main
#
# Each Ansible deploy job sets a per-job resource_group so GitLab serializes it
# across pipelines (two rapid pushes to main can't run the same Ansible play
# against the same hosts at once). Per-job (not one shared group) keeps
# within-pipeline parallelism: jobs targeting different hosts still run
# concurrently. (deploy-terraform relies on its HTTP state lock instead; the
# read-only deploy-verify job needs no serialization.)
# ============================================================================

# ------------------------------------------------------------------------------
# Terraform
# ------------------------------------------------------------------------------

deploy-terraform:
  extends: .terraform-http-backend
  stage: deploy
  interruptible: false
  image:
    name: hashicorp/terraform:1.15
    entrypoint: [""]
  needs:
    - job: validation-gate
    - job: terraform-plan
      artifacts: true
  before_script:
    - !reference [.install-1password-alpine, before_script]
  script:
    - cd terraform/cloudflare
    # -lockfile=readonly: CI must not rewrite .terraform.lock.hcl (provider
    # hashes are committed; a CI-side change would mask supply-chain drift).
    - terraform init -lockfile=readonly
    - |
      export TF_VAR_cloudflare_api_token=$(op read "op://Homelab/Cloudflare Terraform Token/credential")
      export TF_VAR_cloudflare_account_id=$(op read "op://Homelab/Cloudflare Terraform Token/username")
      terraform apply -auto-approve -lock-timeout=120s tfplan
  environment:
    name: production/cloudflare
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - terraform/**/*

# ------------------------------------------------------------------------------
# Ansible Infrastructure
# ------------------------------------------------------------------------------

deploy-ansible-base:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-ansible-base
  needs:
    - job: validation-gate
  variables:
    ROOT_EMAIL_ALIAS: "op://Homelab/Email Config/root_alias"
    SSH_PUBLIC_KEY: "op://Homelab/SSH Key/public key"
    # LOKI_PUSH_USER/PASSWORD now come from the shared .deploy-base (alloy_host
    # needs them on any site.yml-based deploy, not just this job).
  script:
    - cd ansible
    - op run -- ansible-playbook -i inventories/prod playbooks/base.yml
    # base.yml doesn't include node_exporter_host or alloy_host (those live
    # in site.yml); when changes touch them we follow up with a tag-scoped
    # site.yml run so the rules block below stays honest. Idempotent if
    # neither role changed; the tag filter skips everything else in site.yml.
    - op run -- ansible-playbook -i inventories/prod playbooks/site.yml --tags node_exporter_host,alloy_host
  environment:
    name: production/ansible-base
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/base/**/*
        - ansible/roles/nic_tuning/**/*
        # Transitive: resolv_conf is invoked by base when skip_dns_config=false.
        - ansible/roles/resolv_conf/**/*
        # node_exporter_host + alloy_host live in site.yml and run on the
        # same host set base does. The script above runs site.yml with
        # --tags node_exporter_host,alloy_host as a second step so these
        # changes actually deploy when this rule fires.
        - ansible/roles/node_exporter_host/**/*
        - ansible/roles/alloy_host/**/*
        # apt_signed_repo is the shared signed-repo helper alloy_host includes;
        # list it so a helper-only change still triggers the base deploy (same
        # pattern prometheus_exporter uses under deploy-ansible-dns/storage).
        - ansible/roles/apt_signed_repo/**/*
        - ansible/playbooks/base.yml
        - ansible/playbooks/site.yml
        - ansible/inventories/prod/group_vars/proxmox.yml
        - ansible/inventories/prod/host_vars/pve-nas-01.yml

# Proxmox host configuration (qol, postfix_null_client, tailscale)
deploy-ansible-proxmox:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-ansible-proxmox
  needs:
    - job: validation-gate
    - job: deploy-ansible-base
      optional: true
  variables:
    ROOT_EMAIL_ALIAS: "op://Homelab/Email Config/root_alias"
    SSH_PUBLIC_KEY: "op://Homelab/SSH Key/public key"
    SMTP_RELAY_USER: "op://Homelab/SMTP Relay Auth/username"
    SMTP_RELAY_PASSWORD: "op://Homelab/SMTP Relay Auth/password"
    TAILSCALE_AUTH_KEY: "op://Homelab/Tailscale Auth Key/credential"
  script:
    - cd ansible
    # Deploy qol, postfix_null_client, tailscale to proxmox hosts
    - op run -- ansible-playbook -i inventories/prod playbooks/site.yml --limit proxmox --tags qol,postfix_null_client,tailscale
  environment:
    name: production/ansible-proxmox
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/qol/**/*
        - ansible/roles/tailscale/**/*
        - ansible/roles/postfix_null_client/**/*
        # This job RUNS site.yml plays (--limit proxmox); a change to those
        # plays must redeploy too (idempotent, so extra triggers are safe).
        - ansible/playbooks/site.yml

deploy-ansible-firewall:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-ansible-firewall
  needs:
    - job: validation-gate
    - job: deploy-ansible-proxmox
      optional: true
  script:
    - cd ansible
    # Firewall is applied to proxmox hosts, dns, and mail via site.yml
    - op run -- ansible-playbook -i inventories/prod playbooks/site.yml --tags proxmox_firewall
  environment:
    name: production/ansible-firewall
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/proxmox_firewall/**/*
        # Per-guest firewall assignments (guest_security_groups / firewall_ipsets)
        # live ONLY in hosts.yml, not the role — without this an inventory-only
        # firewall edit silently never deploys. site.yml now has a `hosts: k3s`
        # firewall play so --tags proxmox_firewall also reaches k3s guest .fw.
        - ansible/inventories/prod/hosts.yml
        # The proxmox_firewall plays (proxmox/dns/mail/services/k3s — which host
        # groups + tags they target) live in site.yml, which this job RUNS as
        # `site.yml --tags proxmox_firewall`. A change to those plays must also
        # redeploy; without this a future firewall-play edit would merge unrun.
        - ansible/playbooks/site.yml

deploy-ansible-dns:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-ansible-dns
  needs:
    - job: validation-gate
    - job: deploy-ansible-base
      optional: true
  variables:
    ROOT_EMAIL_ALIAS: "op://Homelab/Email Config/root_alias"
    SSH_PUBLIC_KEY: "op://Homelab/SSH Key/public key"
    SMTP_RELAY_USER: "op://Homelab/SMTP Relay Auth/username"
    SMTP_RELAY_PASSWORD: "op://Homelab/SMTP Relay Auth/password"
    ADGUARD_ADMIN_PASSWORD: "op://Homelab/AdGuard Home/password"
    DNS01_SSH_PRIVATE_KEY: "op://Homelab/DNS-01 SSH Key/private key"
    DNS01_SSH_PUBLIC_KEY: "op://Homelab/DNS-01 SSH Key/public key"
  script:
    - cd ansible
    - op run -- ansible-playbook -i inventories/prod playbooks/dns.yml
  environment:
    name: production/ansible-dns
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/unbound/**/*
        - ansible/roles/adguard_home/**/*
        - ansible/roles/adguard_sync/**/*
        - ansible/roles/postfix_null_client/**/*
        # Transitive: resolv_conf used by adguard_home; unbound_exporter
        # invoked from dns.yml.
        - ansible/roles/resolv_conf/**/*
        - ansible/roles/unbound_exporter/**/*
        # unbound_exporter is a thin wrapper over prometheus_exporter (the
        # shared install pipeline), so a change to the shared role must
        # redeploy dns too. It also backs zfs_exporter, so the same path is
        # listed under deploy-ansible-storage.
        - ansible/roles/prometheus_exporter/**/*
        - ansible/playbooks/dns.yml
        # DNS group/host vars: lxc settings, ssh policy, cert distribution
        # targets — all consumed by the dns playbook, so a vars-only change
        # must trigger a redeploy.
        - ansible/inventories/prod/group_vars/dns.yml
        - ansible/inventories/prod/host_vars/dns-01.yml
        - ansible/inventories/prod/host_vars/dns-02.yml

deploy-ansible-storage:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-ansible-storage
  needs:
    - job: validation-gate
    - job: deploy-ansible-base
      optional: true
  variables:
    SSH_PUBLIC_KEY: "op://Homelab/SSH Key/public key"
    SAMBA_NAS_PASSWORD: "op://Homelab/Samba NAS User/password"
  script:
    - cd ansible
    - op run -- ansible-playbook -i inventories/prod playbooks/storage.yml
  environment:
    name: production/ansible-storage
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/nas_storage/**/*
        # zfs_exporter is invoked from storage.yml; nfs_tls is the
        # opt-in NFSv4-over-TLS daemon that runs on the same NAS host
        # plus the k3s NFS clients (cert paths shared with acme_certs).
        - ansible/roles/zfs_exporter/**/*
        # zfs_exporter is a thin wrapper over prometheus_exporter (the
        # shared install pipeline), so a change to the shared role must
        # redeploy storage too. It also backs unbound_exporter, so the
        # same path is listed under deploy-ansible-dns.
        - ansible/roles/prometheus_exporter/**/*
        - ansible/roles/nfs_tls/**/*
        # Declarative storage.cfg + vzdump jobs — runs from storage.yml on
        # pve-nas-01 (cluster-wide config, idempotent).
        - ansible/roles/proxmox_backup/**/*
        - ansible/playbooks/storage.yml

deploy-ansible-mail:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-ansible-mail
  needs:
    - job: validation-gate
    - job: deploy-ansible-dns
      optional: true
  variables:
    ROOT_EMAIL_ALIAS: "op://Homelab/Email Config/root_alias"
    SMTP_RELAY_USER: "op://Homelab/SMTP Relay Auth/username"
    SMTP_RELAY_PASSWORD: "op://Homelab/SMTP Relay Auth/password"
    SMTP_GMAIL_USER: "op://Homelab/SMTP Relay Gmail/username"
    SMTP_GMAIL_PASSWORD: "op://Homelab/SMTP Relay Gmail/password"
    DNS01_SSH_PUBLIC_KEY: "op://Homelab/DNS-01 SSH Key/public key"
  script:
    - cd ansible
    # Mail roles are deployed via site.yml with host targeting
    - op run -- ansible-playbook -i inventories/prod playbooks/site.yml --limit mail
  environment:
    name: production/ansible-mail
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/smtp_relay/**/*
        - ansible/roles/postfix_null_client/**/*
        # Mail group vars: relay/null-client settings consumed by the
        # site.yml --limit mail run, so a vars-only change must trigger
        # a redeploy (mirrors group_vars/dns.yml on the dns job).
        - ansible/inventories/prod/group_vars/mail.yml
        # This job RUNS site.yml plays (--limit mail); a change to those
        # plays must redeploy too.
        - ansible/playbooks/site.yml

deploy-ansible-certs:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-ansible-certs
  variables:
    # acme_certs asserts acme_email on dns-01, and acme_email <- admin_email <-
    # ROOT_EMAIL_ALIAS. Every other deploy job injects this op:// ref; it was
    # missing here, so `op run` left admin_email undefined and the play failed
    # the "Validate required acme.sh variables" assert (the SSH-key fields, by
    # contrast, are handled via `op read` in script below).
    ROOT_EMAIL_ALIAS: "op://Homelab/Email Config/root_alias"
  needs:
    - job: validation-gate
    - job: deploy-ansible-dns
      optional: true
    - job: deploy-ansible-mail
      optional: true
  script:
    - cd ansible
    # `op run` does NOT inject an SSH-KEY item's private/public key fields into
    # the environment for a service-account token (it resolves them to empty),
    # so acme_certs saw DNS01_SSH_* unset. `op read` DOES resolve them — the same
    # call .deploy-base uses to fetch the SSH connection key — so read them
    # explicitly here, then run ansible. (Not in `variables:` as op:// refs,
    # because that path goes through op run's broken SSH-key injection.)
    - |
      DNS01_SSH_PRIVATE_KEY="$(op read 'op://Homelab/DNS-01 SSH Key/private key')"
      DNS01_SSH_PUBLIC_KEY="$(op read 'op://Homelab/DNS-01 SSH Key/public key')"
      if [ -z "$DNS01_SSH_PRIVATE_KEY" ] || [ -z "$DNS01_SSH_PUBLIC_KEY" ]; then
        echo "ERROR: op read returned empty DNS-01 SSH Key material (check the service account's vault access to 'DNS-01 SSH Key')." >&2
        exit 1
      fi
      export DNS01_SSH_PRIVATE_KEY DNS01_SSH_PUBLIC_KEY
    # Certificate roles via site.yml; op run resolves the remaining op:// vars.
    - op run -- ansible-playbook -i inventories/prod playbooks/site.yml --limit dns --tags acme_certs
  environment:
    name: production/ansible-certs
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/acme_certs/**/*
        # host_vars/dns-01.yml carries the pinned host_key entries that
        # acme_certs uses for cert distribution; a change to those
        # fingerprints needs a redeploy.
        - ansible/inventories/prod/host_vars/dns-01.yml
        # This job RUNS site.yml plays (--limit dns --tags acme_certs); a
        # change to those plays must redeploy too.
        - ansible/playbooks/site.yml

# ------------------------------------------------------------------------------
# Ansible Applications (VMs/LXC)
# ------------------------------------------------------------------------------

deploy-plex:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-plex
  needs:
    - job: validation-gate
    - job: deploy-ansible-base
      optional: true
  variables:
    # plex.yml runs postfix_null_client, which renders mail_aliases
    # (root -> admin_email -> ROOT_EMAIL_ALIAS); without it the aliases
    # task fails with AnsibleUndefinedVariable. Mirrors the other deploy jobs.
    ROOT_EMAIL_ALIAS: "op://Homelab/Email Config/root_alias"
    SSH_PUBLIC_KEY: "op://Homelab/SSH Key/public key"
    SMTP_RELAY_USER: "op://Homelab/SMTP Relay Auth/username"
    SMTP_RELAY_PASSWORD: "op://Homelab/SMTP Relay Auth/password"
    # Plex custom-certificate PFX passphrase (matches the value configured
    # in Plex Settings -> Network -> "Custom certificate encryption key").
    # The plex role asserts this is non-empty before re-keying.
    PLEX_PFX_PASSPHRASE: "op://Homelab/Plex Custom Certificate/password"
  script:
    - cd ansible
    - op run -- ansible-playbook -i inventories/prod playbooks/plex.yml
  environment:
    name: production/plex
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/plex/**/*
        # apt_signed_repo: shared signed-repo helper plex includes (Debian
        # non-free repo pin) — a helper-only change must still deploy plex.
        - ansible/roles/apt_signed_repo/**/*
        - ansible/roles/proxmox_lxc/**/*
        - ansible/playbooks/plex.yml

deploy-gitlab:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-gitlab
  needs:
    - job: validation-gate
    - job: deploy-ansible-base
      optional: true
  variables:
    # GitLab role reads secrets from env vars via lookup('ansible.builtin.env', ...)
    # op run resolves op:// references in environment variables at runtime
    ROOT_EMAIL_ALIAS: "op://Homelab/Email Config/root_alias"
    SMTP_RELAY_USER: "op://Homelab/SMTP Relay Auth/username"
    SMTP_RELAY_PASSWORD: "op://Homelab/SMTP Relay Auth/password"
    SSH_PUBLIC_KEY: "op://Homelab/SSH Key/public key"
    GITLAB_ROOT_PASSWORD: "op://Homelab/GitLab/root-password"
    GITLAB_SAML_CERT_FINGERPRINT: "op://Homelab/GitLab SSO/saml-cert-fingerprint"
    # Personal Access Token used by the role's Web IDE Application Settings
    # API calls (vscode_extension_marketplace_*). The gitlab role asserts
    # this is set and resolved (not still an op:// reference) before the
    # API block runs.
    GITLAB_API_TOKEN: "op://Homelab/GitLab API Token/credential"
  script:
    - cd ansible
    - op run -- ansible-playbook -i inventories/prod playbooks/gitlab.yml
    - |
      # Wait for GitLab API to come back after reconfigure (up to 5 min)
      echo "Waiting for GitLab API..."
      gitlab_healthy=false
      start_ts=$(date +%s)
      while true; do
        if curl -sf --max-time 5 https://git.ericsweiss.com/-/readiness > /dev/null 2>&1; then
          elapsed=$(( $(date +%s) - start_ts ))
          echo "GitLab API healthy after ${elapsed}s"
          gitlab_healthy=true
          break
        fi
        elapsed=$(( $(date +%s) - start_ts ))
        if [ "$elapsed" -ge 300 ]; then break; fi
        [ $((elapsed % 20)) -lt 3 ] && echo "Still waiting... (${elapsed}s)"
        sleep 2
      done
      if [ "$gitlab_healthy" != "true" ]; then
        echo "ERROR: GitLab API did not become healthy in 300s"
        exit 1
      fi
  environment:
    name: production/gitlab
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/gitlab/**/*
        # apt_signed_repo: shared signed-repo helper the gitlab role includes
        # for the GitLab omnibus apt repo — a helper-only change must deploy it.
        - ansible/roles/apt_signed_repo/**/*
        # zvol_mount is invoked by the gitlab role for the
        # /mnt/gitlab-repos persistent volume.
        - ansible/roles/zvol_mount/**/*
        - ansible/playbooks/gitlab.yml
        # gitlab_servers group vars (postfix null-client, kernel tuning) are
        # consumed by the gitlab playbook, so a vars-only change must redeploy.
        - ansible/inventories/prod/group_vars/gitlab_servers.yml
        # all.yml triggers deploy for gitlab_version changes; may also trigger
        # on unrelated version bumps but the playbook is idempotent and safe to re-run
        - ansible/inventories/prod/group_vars/all.yml

deploy-home-assistant-config:
  stage: deploy
  extends: .deploy-base
  resource_group: deploy-home-assistant-config
  needs:
    - job: validation-gate
    - job: deploy-ansible-base
      optional: true
  variables:
    # Home Assistant role reads secrets from env vars via lookup('ansible.builtin.env', ...)
    SMTP_RELAY_USER: "op://Homelab/SMTP Relay Auth/username"
    SMTP_RELAY_PASSWORD: "op://Homelab/SMTP Relay Auth/password"
    ADMIN_EMAIL: "op://Homelab/Email Config/root_alias"
    HA_OIDC_CLIENT_ID: "op://Homelab/Home Assistant SSO/authentik-client-id"
    HA_OIDC_CLIENT_SECRET: "op://Homelab/Home Assistant SSO/authentik-client-secret"
  script:
    - cd ansible
    - op run -- ansible-playbook -i inventories/prod -e "ansible_become=false" playbooks/home-assistant.yml
  environment:
    name: production/home-assistant
  rules:
    - !reference [.skip-schedule-web, rules]
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - ansible/roles/home_assistant/**/*
        - ansible/playbooks/home-assistant.yml
        - ansible/inventories/prod/host_vars/home.yml

deploy-gitlab-verify:
  stage: deploy
  extends: .deploy-base
  needs:
    - job: validation-gate
    - job: deploy-gitlab
      optional: true
    # Ingress/runner deploys are now Flux-managed; reconciliation typically
    # completes within 1-2 minutes of the push that triggered this pipeline.
  script:
    - |
      echo "=== GitLab Verification ==="
      # Each check records its own failure instead of exiting immediately —
      # an operator wants to see EVERY failing component in one run, not
      # stop at the first one. Final summary surfaces exit code.
      # With allow_failure: true on the job, a non-zero exit yields a yellow
      # warning-style status in the pipeline UI but doesn't block the pipeline.
      EXIT=0

      echo "Testing GitLab web UI..."
      if ! curl_err=$(curl -sf --max-time 30 "https://git.ericsweiss.com/-/readiness" 2>&1); then
        echo "ERROR: GitLab readiness check failed: $curl_err"
        EXIT=1
      else
        echo "GitLab web UI: OK"
      fi

      echo "Testing Container Registry..."
      REGISTRY_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 https://registry.git.ericsweiss.com/v2/)
      if [ "$REGISTRY_CODE" = "200" ] || [ "$REGISTRY_CODE" = "401" ]; then
        echo "Container registry: OK (HTTP $REGISTRY_CODE)"
      else
        echo "Container registry: FAILED (HTTP $REGISTRY_CODE)"
        EXIT=1
      fi

      echo "Testing SSH connectivity..."
      if timeout 5 bash -c 'cat < /dev/null > /dev/tcp/192.168.0.153/22' 2>/dev/null; then
        echo "SSH port 22: OK"
      else
        echo "ERROR: SSH port 22 on 192.168.0.153 not reachable"
        EXIT=1
      fi

      echo ""
      echo "=== GitLab Verification Complete (exit=$EXIT) ==="
      exit $EXIT
  environment:
    name: production/gitlab-verify
  rules:
    # Inherit deploy-gitlab's rules verbatim so this verify job appears in
    # the pipeline if and only if deploy-gitlab does. Avoids drift between
    # deploy and verify trigger paths.
    - !reference [deploy-gitlab, rules]
  allow_failure: true

# ------------------------------------------------------------------------------
# Post-Deploy Verification
# ------------------------------------------------------------------------------

deploy-verify:
  stage: deploy
  extends: .k3s-deploy-base
  needs:
    - job: validation-gate
    - job: deploy-terraform
      optional: true
    - job: deploy-ansible-base
      optional: true
    - job: deploy-ansible-proxmox
      optional: true
    - job: deploy-ansible-firewall
      optional: true
    - job: deploy-ansible-dns
      optional: true
    - job: deploy-ansible-storage
      optional: true
    - job: deploy-ansible-mail
      optional: true
    - job: deploy-ansible-certs
      optional: true
    - job: deploy-plex
      optional: true
    - job: deploy-gitlab
      optional: true
    - job: deploy-home-assistant-config
      optional: true
    - job: deploy-gitlab-verify
      optional: true
    # Kubernetes workloads (k3s platform, Authentik, downloads, recipes,
    # ingress routes, runners) are Flux-managed; Flux typically reconciles
    # within 1-2 minutes of this push. deploy-verify checks cluster health
    # below and reports Flux reconciliation state via `flux get all -A`.
  script:
    - |
      # Install flux CLI alongside the kubectl already provisioned by
      # .k3s-deploy-base. Flux is the source of truth for k8s workload state,
      # so verification queries Flux CRs rather than individual Deployments.
      # Version + checksum are pinned together (supply-chain); bump both in
      # one change. check-versions tracks flux_version in all.yml, and the
      # guard below fails the job if this pin drifts from it.
      FLUX_VERSION="2.9.0"
      grep -q "flux_version: ${FLUX_VERSION}" kubernetes/infrastructure/sources/versions-configmap.yaml \
        || { echo "FLUX_VERSION ${FLUX_VERSION} drifted from versions-configmap flux_version"; exit 1; }
      curl -fsSL "https://github.com/fluxcd/flux2/releases/download/v${FLUX_VERSION}/flux_${FLUX_VERSION}_linux_amd64.tar.gz" -o /tmp/flux.tar.gz
      echo "7aa9779fc7ec6d030e609dab7dbed8c6fc9e3a7f4670834ad02369268aaf341f  /tmp/flux.tar.gz" | sha256sum -c -
      tar xzf /tmp/flux.tar.gz -C /usr/local/bin flux
      flux --version
    - bash scripts/deploy-verify.sh
  rules:
    # Inherit each deploy-* job's rules verbatim so this verify job appears
    # in the pipeline if and only if at least one deploy-* job does.
    # GitLab evaluates rules in order, first match wins; each !reference
    # expansion contributes that job's `if:`+`changes:` triplet, and the
    # `if: schedule|web when: never` rules at the top of each block apply
    # equally to verify (we don't want verify on schedules/web pipelines
    # any more than the deploys themselves).
    - !reference [deploy-terraform, rules]
    - !reference [deploy-ansible-base, rules]
    - !reference [deploy-ansible-proxmox, rules]
    - !reference [deploy-ansible-firewall, rules]
    - !reference [deploy-ansible-dns, rules]
    - !reference [deploy-ansible-storage, rules]
    - !reference [deploy-ansible-mail, rules]
    - !reference [deploy-ansible-certs, rules]
    - !reference [deploy-plex, rules]
    - !reference [deploy-gitlab, rules]
    - !reference [deploy-home-assistant-config, rules]
    # Flux-only changes (kubernetes/ manifests with no Ansible or all.yml edit)
    # match none of the deploy-* rules above, so the live Flux reconcile +
    # server-side dry-run below would never run for the most common GitOps
    # change. Trigger verify directly on kubernetes/ edits. schedule/web are
    # already excluded by the when:never guards in the referenced blocks
    # (rules are first-match-wins, and those guards come first).
    - if: '$CI_COMMIT_BRANCH == "main" && $OP_SERVICE_ACCOUNT_TOKEN'
      changes:
        - kubernetes/**/*
  # No allow_failure: the verification explicitly fails if nodes aren't Ready,
  # Flux reconciliation times out, any Flux resource is not Ready, server-side
  # dry-run rejects manifests, or GitLab is unreachable. ExternalSecret
  # readiness is a hard failure on steady-state clusters (all Kustomizations
  # were Ready pre-reconcile) and a warning during bootstrap/recovery.

# ============================================================================
# MAINTENANCE STAGE - Manual approval required
# All maintenance jobs use resource_group to prevent concurrent execution.
# Only one maintenance job can run at a time across the entire infrastructure.
#
# Recommended execution order (when running multiple):
#   1. update-packages       — OS packages on all hosts (may reboot)
#   2. update-applications   — App upgrades (needs clean OS)
#   3. update-k3s-nodes      — k3s binary upgrade (needs stable OS)
#   4. k3s-provision         — Only when adding new VMs
#   5. proxmox-ha            — HA rules (rarely changes)
#   6. home-assistant-restart — Only when needed
#   7. verify                — Always last
# ============================================================================

# 1. OS package updates - MANUAL (all hosts, auto-reboot in CI)
# For interactive mode, use: task maintenance:update-packages
maintenance-update-packages:
  stage: maintenance
  extends: .maintenance-base
  resource_group: infrastructure-maintenance
  script:
    - cd ansible
    - bash ../scripts/maintenance-run-with-verify.sh
        op run -- ansible-playbook -i inventories/prod playbooks/maintenance/update-packages.yml -e auto_reboot=true -e self_reboot_delay=5400
  environment:
    name: production/maintenance-packages
    action: prepare
  rules:
    - !reference [.maintenance-manual-rules, rules]

# 2. Application updates - MANUAL (AdGuard, Tailscale, Plex)
maintenance-update-applications:
  stage: maintenance
  extends: .maintenance-base
  resource_group: infrastructure-maintenance
  variables:
    ROOT_EMAIL_ALIAS: "op://Homelab/Email Config/root_alias"
    SSH_PUBLIC_KEY: "op://Homelab/SSH Key/public key"
    SMTP_RELAY_USER: "op://Homelab/SMTP Relay Auth/username"
    SMTP_RELAY_PASSWORD: "op://Homelab/SMTP Relay Auth/password"
    ADGUARD_ADMIN_PASSWORD: "op://Homelab/AdGuard Home/password"
    TAILSCALE_AUTH_KEY: "op://Homelab/Tailscale Auth Key/credential"
  script:
    - cd ansible
    - bash ../scripts/maintenance-run-with-verify.sh
        op run -- ansible-playbook -i inventories/prod playbooks/maintenance/update-applications.yml
  environment:
    name: production/maintenance-applications
    action: prepare
  rules:
    - !reference [.maintenance-manual-rules, rules]

# 3. K3s node updates - MANUAL (cordon/upgrade/restart; no drain — kernel reboots via kured)
maintenance-update-k3s-nodes:
  stage: maintenance
  extends: .maintenance-base
  resource_group: infrastructure-maintenance
  variables:
    K3S_TOKEN: "op://Homelab/K3s Cluster Token/credential"
    # Lower-privilege agent token (ANS-D-2). Without it, group_vars/k3s.yml
    # falls back to the cluster token and node upgrades would rewrite the
    # agents' env files with the full token. See docs/19 "Agent token".
    K3S_AGENT_TOKEN: "op://Homelab/K3s Agent Token/credential"
  script:
    - cd ansible
    - bash ../scripts/maintenance-run-with-verify.sh
        op run -- ansible-playbook -i inventories/prod playbooks/maintenance/update-k3s-nodes.yml
  environment:
    name: production/k3s-nodes
    action: prepare
  rules:
    - !reference [.maintenance-manual-rules, rules]

# 4. K3s VM provisioning - MANUAL (creates/modifies VMs)
maintenance-k3s-provision:
  stage: maintenance
  extends: .maintenance-base
  resource_group: infrastructure-maintenance
  variables:
    SSH_PUBLIC_KEY: "op://Homelab/SSH Key/public key"
    K3S_TOKEN: "op://Homelab/K3s Cluster Token/credential"
    # Lower-privilege agent token (ANS-D-2). Servers advertise it (agent-token)
    # and agents join with it instead of the cluster token. See docs/19
    # "Agent token".
    K3S_AGENT_TOKEN: "op://Homelab/K3s Agent Token/credential"
  script:
    - cd ansible
    # k3s-provision is two playbooks; chain them inside `bash -c` so the
    # wrapper sees a single command unit. set -e inside the inline script
    # ensures the second playbook is skipped if the first fails.
    - bash ../scripts/maintenance-run-with-verify.sh bash -c
        'set -e;
         op run -- ansible-playbook -i inventories/prod playbooks/k3s-provision-vms.yml;
         op run -- ansible-playbook -i inventories/prod playbooks/k3s.yml'
  environment:
    name: production/k3s-provision
    action: prepare
  rules:
    - !reference [.maintenance-manual-rules, rules]

# NOTE: maintenance-update-full removed from CI — superseded by:
#   maintenance-update-packages (broader host coverage) + maintenance-update-applications
# The Taskfile still has task maintenance:update-full for local use.

# 5. Proxmox HA - MANUAL (affects VM placement)
maintenance-proxmox-ha:
  stage: maintenance
  extends: .maintenance-base
  resource_group: infrastructure-maintenance
  script:
    - cd ansible
    - bash ../scripts/maintenance-run-with-verify.sh
        op run -- ansible-playbook -i inventories/prod playbooks/proxmox-ha.yml
  environment:
    name: production/proxmox-ha
    action: prepare
  rules:
    - !reference [.maintenance-manual-rules, rules]

# 6. Home Assistant restart - MANUAL (restarts HA VM)
maintenance-home-assistant-restart:
  stage: maintenance
  extends: .maintenance-base
  resource_group: infrastructure-maintenance
  script:
    # The HA restart sub-script handles ha-manager + readiness loop;
    # the wrapper guarantees verify runs after.
    - bash scripts/maintenance-run-with-verify.sh bash scripts/maintenance-ha-restart.sh
  environment:
    name: production/home-assistant-restart
    action: prepare
  rules:
    - !reference [.maintenance-manual-rules, rules]

# Optional: single-button "run all 6 ops in sequence". Delegates the
# 6-op sequence to scripts/maintenance-all-ops.sh and wraps it in
# maintenance-run-with-verify.sh so the post-maintenance verify
# always runs at the end (even if a sub-op failed).
maintenance-run-all:
  stage: maintenance
  extends: .maintenance-base
  resource_group: infrastructure-maintenance
  variables:
    # Union of secrets needed by the six ops invoked below.
    ROOT_EMAIL_ALIAS: "op://Homelab/Email Config/root_alias"
    SSH_PUBLIC_KEY: "op://Homelab/SSH Key/public key"
    SMTP_RELAY_USER: "op://Homelab/SMTP Relay Auth/username"
    SMTP_RELAY_PASSWORD: "op://Homelab/SMTP Relay Auth/password"
    ADGUARD_ADMIN_PASSWORD: "op://Homelab/AdGuard Home/password"
    TAILSCALE_AUTH_KEY: "op://Homelab/Tailscale Auth Key/credential"
    K3S_TOKEN: "op://Homelab/K3s Cluster Token/credential"
    # Lower-privilege agent token (ANS-D-2) — required by the k3s node-update
    # and provision ops below, or they'd fall back to the cluster token.
    K3S_AGENT_TOKEN: "op://Homelab/K3s Agent Token/credential"
  script:
    # The all-ops sub-script handles the 6 ops; the wrapper guarantees
    # verify runs after, even if any op failed.
    - bash scripts/maintenance-run-with-verify.sh bash scripts/maintenance-all-ops.sh
  environment:
    name: production/maintenance-run-all
    action: prepare
  rules:
    - !reference [.maintenance-manual-rules, rules]

# 7. Post-maintenance verification - MANUAL ad-hoc cluster health check
# The maintenance ops + maintenance-run-all already invoke
# scripts/post-maintenance-verify.sh as the last step of their own job,
# so verify runs strictly after each op without any cross-job needs/stage
# coordination. This standalone job is just an ad-hoc "check cluster
# health right now" button; click it any time to re-run the same script
# without triggering an actual maintenance op.
maintenance-verify:
  stage: maintenance
  extends: .maintenance-base
  resource_group: infrastructure-maintenance
  script:
    - bash scripts/post-maintenance-verify.sh
  rules:
    - !reference [.maintenance-manual-rules, rules]

# ============================================================================
# VERSION CHECK - Runs early, soft-fail on PRs
# ============================================================================

version-check:
  stage: lint
  image: python:3.11
  before_script:
    # Install the 1Password CLI only when OP_SERVICE_ACCOUNT_TOKEN is present
    # (forks / outside MRs don't get the masked variable). The CLI is only
    # needed for the MR auto-comment path; without the token the job still
    # runs and still emits the version-report.json artifact. We inline the
    # apt install here (rather than `extends`/`!reference` .install-1password)
    # so it can be conditional — a YAML `!reference` can't live inside a shell
    # `if`, and unconditionally installing it on every schedule/MR/main run
    # would add a useless apt round-trip when no token is available.
    - |
      if [ -n "$OP_SERVICE_ACCOUNT_TOKEN" ]; then
        apt-get update -qq
        apt-get install -y -qq curl gnupg
        curl -sS https://downloads.1password.com/linux/keys/1password.asc | \
          gpg --dearmor --output /usr/share/keyrings/1password-archive-keyring.gpg
        echo "deb [arch=amd64 signed-by=/usr/share/keyrings/1password-archive-keyring.gpg] https://downloads.1password.com/linux/debian/amd64 stable main" \
          > /etc/apt/sources.list.d/1password.list
        apt-get update -qq && apt-get install -y -qq 1password-cli
        op --version
      fi
  script:
    - pip install --quiet "pyyaml==${PYYAML_VERSION}" requests==2.32.3
    # GITLAB_API_TOKEN drives version-check-ci.py's post_mr_comment(). Without
    # it the script gracefully skips the comment and only writes the
    # version-report.json artifact (graceful-skip lives in version-check-ci.py).
    # Gate the op read on the token so non-token pipelines still run cleanly.
    - |
      if [ -n "$OP_SERVICE_ACCOUNT_TOKEN" ]; then
        export GITLAB_API_TOKEN=$(op read "op://Homelab/GitLab API Token/credential")
      fi
    - python3 ./scripts/version-check-ci.py
  artifacts:
    paths:
      - version-report.json
    expire_in: 7 days
    when: always
  rules:
    # Run on schedule (weekly check). Soft-fail: rc=1 just means updates are
    # available, which must not red the scheduled pipeline.
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      allow_failure: true
    # Manual trigger on web pipelines (must precede main rule to avoid auto-run).
    # allow_failure: rules-based `when: manual` defaults to allow_failure: false,
    # which would leave every web pipeline "blocked" until this job is played.
    - if: '$CI_PIPELINE_SOURCE == "web"'
      when: manual
      allow_failure: true
    # Run on MRs as informational (soft-fail)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      allow_failure: true
    # Run on pushes to main as informational (soft-fail)
    - if: '$CI_COMMIT_BRANCH == "main"'
      allow_failure: true
