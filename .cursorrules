# Cursor Rules for weisssrv Homelab

You are working in the weisssrv homelab infrastructure repository. This repository manages a production Proxmox-based homelab using Ansible, Terraform, Kubernetes (k3s), and GitOps principles.

**Tech Stack**: Ansible, Terraform, k3s, ZFS, Proxmox VE, Debian 13 (trixie)

See `CLAUDE.md` for the complete project overview, architecture, IP allocation, commands, storage architecture, secrets management, version management, and documentation index.

## Critical Rules

### NEVER

1. **Commit secrets** - All secrets use 1Password references (`op://Homelab/Item/field`)
2. **Automate ZFS pool creation/destruction** - Too critical, manual only
3. **Skip deployment verification** - Always run `task deploy:verify` after changes
4. **Hardcode credentials** - Use 1Password references exclusively

### ALWAYS

1. **Test with dry run first** - `task deploy:check` before deploying
2. **Use fully qualified collection names** - `ansible.builtin.apt` not `apt`
3. **Follow existing patterns** - Check similar roles for conventions
4. **Update documentation** - When changing behavior, update docs/
5. **Use snake_case** - For all Ansible variables

## Code Conventions

### Ansible Tasks

```yaml
# Good example
- name: Deploy AdGuard Home configuration
  ansible.builtin.template:
    src: AdGuardHome.yaml.j2
    dest: "{{ adguard_install_path }}/AdGuardHome.yaml"
    owner: adguard
    group: adguard
    mode: "0600"
  notify: Restart AdGuardHome
  no_log: true  # Contains passwords
```

### Variable Organization

**Precedence** (low to high):
1. `group_vars/all.yml` - Global defaults, version pins, secrets
2. `group_vars/<group>.yml` - Group-specific config
3. `host_vars/<host>.yml` - Host-specific overrides

**Naming**: Role-specific (`adguard_http_port`), Global (`internal_domain`), Secrets (in `secrets:` dict with `op://`)

### Secrets

```yaml
# In group_vars/all.yml
secrets:
  smtp_gmail_password: "op://Homelab/SMTP Relay Gmail/password"

# Usage - always add no_log: true for password tasks
```

## Common Workflows

### Add New Service
See `docs/18-bootstrap-new-systems.md`

### Update Application Version
1. `task maintenance:check-versions` - Check for updates
2. `task maintenance:update-version SERVICE=<name>` - Update version in all.yml
3. Deploy with appropriate task (see `CLAUDE.md` for full task list)
4. `task deploy:verify` and/or `task k3s:status`

## Ansible Patterns

### Handler Pattern
```yaml
# tasks/main.yml
- name: Deploy configuration
  ansible.builtin.template:
    src: config.j2
    dest: /etc/service/config
  notify: Restart service

# handlers/main.yml
- name: Restart service
  ansible.builtin.service:
    name: service
    state: restarted
```

### Service Pattern
```yaml
- name: Install packages
  ansible.builtin.apt:
    name: [package1, package2]
    state: present
    update_cache: true

- name: Create service user
  ansible.builtin.user:
    name: serviceuser
    system: true
    create_home: false
    shell: /usr/sbin/nologin

- name: Deploy systemd unit
  ansible.builtin.template:
    src: service.service.j2
    dest: /etc/systemd/system/service.service
  notify: [Reload systemd, Restart service]

- name: Enable service
  ansible.builtin.systemd:
    name: service
    enabled: true
    state: started
```

## Key References

- **Full project docs**: `CLAUDE.md`
- **Main playbook**: `ansible/playbooks/site.yml`
- **Inventory**: `ansible/inventories/prod/hosts.yml`
- **Global vars / versions**: `ansible/inventories/prod/group_vars/all.yml`
- **Task runner**: `Taskfile.yml` (run `task --list` for all tasks)
- **K3s manifests**: `kubernetes/apps/` and `kubernetes/bootstrap/`
- **All docs**: `docs/` (29 files covering hardware through GitLab migration)
